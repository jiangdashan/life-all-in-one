# v75 更新说明（线上版本 /59/）

## 一、手机上找不到倒数日 —— 已修复

**原因**：手机端的左侧图标栏是**独立于电脑上侧栏的第二份列表**。上一版新增「倒数日」时，只往电脑侧栏加了入口，手机那份没同步，所以电脑能进、手机永远看不到。

**修法**：在手机导航的「回本」后面补上「倒数」（图标早已存在）。手机端现在共 16 个入口，与电脑一致。

**已加防复发检查**：自动化测试会断言手机导航必须是 16 项且包含倒计时，以后再加新模块漏改会直接测出来。

---

## 二、P1：建立视觉主次（三档权重）

核心问题是**每个页面里所有东西一样大**，眼睛不知道先看哪。现在每个模块顶部固定只放三样，从上到下权重递减：

| 档位 | 干什么 | 字号 | 例子 |
|---|---|---|---|
| 主数字 | 这个模块此刻最重要的一件事 | 30px（手机 26px） | 本月支出 ¥2,340 |
| 一句结论 | 用大白话说清变化 | 13px | 比上月少花 210 元 |
| 细节 | 列表、日期、备注 | 11–12px 灰 | 9月22日 · 餐饮 |

已覆盖的 10 个模块，各自的「主数字 → 一句话」是：

| 模块 | 主数字 | 一句话 |
|---|---|---|
| 记账理财 | 本月支出 ¥X | 比上月多／少花了 ¥X |
| 习惯健康 | 今日打卡 3 / 5 | 最长连续 12 天（还没记录时：今天完成一点，就算前进） |
| 减脂健身 | 当前体重 58.4 kg | 比上次轻 0.6 kg |
| 睡眠记录 | 昨晚睡了 7 小时 20 分 | 比近 7 天平均多睡 30 分钟 |
| 学习记录 | 近 7 天学习 6 小时 | 今天 50 分钟 · 已完成 2 个番茄 |
| 日程统筹 | 待办未完成 4 件 | 其中 2 件今天该做了 |
| 待买清单 | 待买 3 件 | 本月已买 5 件 |
| 每日饮食 | 今日摄入 1,620 kcal | 已经记下 3 餐 |
| 物品收纳 | 库存 12 种 | 34 件在库 · 放在 4 个位置 |
| 回本记录 | 大件投入 ¥8,600 | 3 件在用，越用越回本 |

**没数据的模块**（比如从没记过睡眠）这个区块会自动隐藏，不占地方。

心情、书影音、倒数日、时光档案四个模块没加，因为它们各自本来就有统计卡／日历／大卡片，再叠一个反而重复。

### 顺带做的三件小事

1. **数字不再左右跳动**：全站数字改成等宽字形（倒数日先试过，效果不错，这次推广到全部）。
2. **空状态可以说第二句**：`empty()` 函数支持再加一行灰色小字提示，现有调用点不受影响，以后可以给空列表补上「点这里添加第一件」这类引导。
3. **配色没动**：仍然是不加彩色、不加阴影、不加渐变的朴素风格，主次全靠字号和留白区分。

---

## 三、验证

- 新增测试 **42 项全过**，其中包括：模拟真实点击手机端「倒数」按钮能切到倒数日页、十个模块在空数据下全部隐藏、有数据后主数字和结论句内容正确、主数字字号确实大于结论句字号。
- 全站回归约 **600 项 0 失败**（v57 27、v63 32、v65 41、v67 22、v68 28、v69 39、v70 21、v71 28、v72 39、v73 56、v74 43、v75 42，以及经期系列 216 项）。
- 线上部署后 26 个关键标记全部命中，并额外下载线上文件复核：手机导航确认为 16 项且含倒计时、10 个结论区块都在。

## 四、过程中值得一提的

首次部署报了一个标记缺失，我没有直接去改代码，而是先把线上的文件下载下来核对 —— 确认改动其实已经生效了，问题在于**发布平台会自动给元素补属性**，导致用整段标签做断言必然匹配不上。改成用短片段断言后恢复正常（顺带把这个坑记进了项目记忆，避免以后重复踩）。
