# v52 · 把工作台装到手机桌面

> 线上版本：静态 **/29/**（v52）· 链接 https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy

---

## 一、结论：可以「装」，但有边界

| 方式 | 可行性 | 实际效果 |
|------|--------|----------|
| ① 添加到主屏幕（桌面图标） | ✅ 可行，已为你加好引导 | 桌面出现图标，**点一下直接进入工作台**，不用再找链接 |
| ② 全屏独立 App（无地址栏、自定义图标、可离线） | ⚠️ 受平台限制，需外加一层「外壳页」才可能实现 | 见第三节 |
| ③ 打包成真正的安装包（APK / IPA） | ❌ 当前环境不可行 | Windows 上无法构建 iOS；Android 需额外搭建构建与签名环境 |

**推荐直接用 ① —— 它已经能满足「快速进入项目操作」这个目标。**

---

## 二、为什么不能一步到位变成原生 App（排查证据）

1. 分享链接 `https://workbuddy.link/p/xxx` 打开的其实是平台的**外壳页**，你的工作台是被装在这个外壳的 **iframe** 里运行的。
2. 页面里的数据接口 `__SMART_PAGE__.database` 是一套**父子页通信桥**：工作台只能通过外壳页去读写云端数据表。脱离外壳单独打开 → 数据读不出来。
3. 平台在页面里注入的守卫脚本写明：**若被直接打开（非 iframe），会显示「安全限制」并停止加载页面**。也就是说静态直链（`workbuddy-space-static.../29/life-all-in-one.html`）直接访问会被拦截。
4. 「添加到主屏幕」时，iOS / Android 读取的是**最外层文档**的配置；最外层是平台外壳、不由我们控制 → 因此桌面图标与全屏模式都无法由工作台自己决定。

结论：**「桌面图标 + 一键进入」完全可行；「全屏原生体验」要绕一层，且依赖平台是否允许被嵌套。**

---

## 三、方式 ② 的思路（需要你确认后我再动手）

做一个极简「App 外壳页」并单独发布成一个在线应用：

```
外壳页（顶层，我们自己的图标 + 全屏配置）
   └── iframe → 平台分享链接（外壳页）→ iframe → 你的工作台（数据桥照常工作）
```

- 好处：可以拥有**自己的桌面图标**、**全屏无地址栏**，安卓还会出现「安装应用」入口。
- 风险：多套一层 iframe，个别交互（如底层滚动、键盘顶起）可能有细微差异；且需要平台允许被嵌套（已初步确认无禁止响应头，但**必须你在手机真机上实测**）。
- 代价：需要额外发布一个在线应用来承载外壳页。

---

## 四、本次 v52 已上线的内容

1. **首屏提示条**：手机上首次进入时，底部出现「把它装到手机桌面 → 怎么装」，点一次就带你去完整步骤；点「×」后不再打扰。
2. **设置 → 外观 里的「装到手机桌面」模块**（电脑端也能看到）：
   - 一键**复制链接**；
   - 按你的设备自动显示步骤（iPhone Safari / iPhone 微信 / Android Chrome / Android 微信 / 电脑各一套）；
   - 说明：同一台设备 7 天内不用重新输密码，换设备或超期输一次密码，数据自动从云端同步回来。
3. **桌面图标相关配置**已写入页面（`apple-mobile-web-app-capable`、`apple-mobile-web-app-title`、内联图标等），为方式 ② 打好基础。
4. 已从桌面图标进入时（全屏模式），提示条自动隐藏，说明文字改为「已从桌面图标进入」。

质量保障：`node --check` 通过；新增冒烟测试 `_tools/_smoke_v52.js` **36/36 通过**；回归 v51(33) / v49(30) / v48(41) / v42(13) 全绿；线上 18/18 标记命中。

---

## 五、怎么装（照做即可）

**iPhone（Safari）**
1. 用 Safari 打开 https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy
2. 点底部工具栏的「分享」按钮（方框向上箭头）
3. 菜单里向下滑，选「添加到主屏幕」
4. 右上角「添加」→ 桌面出现图标，点开即进

**iPhone（微信里打开时）**
1. 点右上角「…」→「在 Safari 中打开」
2. 再按上面的 Safari 步骤

**Android（Chrome）**
1. 打开链接 → 点右上角「⋮」
2. 选「添加到主屏幕」或「安装应用」
3. 确认 → 桌面出现图标

> 提示：数据存在云端数据表里，桌面图标只是「更快的入口」，不影响两端数据的同步关系。


---

## v53 · 鸿蒙（HarmonyOS）专属引导（2026-09-17 追加，线上 /30/）

### 这次改了什么
1. **自动识别鸿蒙**：判据是 UA 里出现 `HarmonyOS` / `OpenHarmony` / `HuaweiBrowser` 任一。
   注意 HarmonyOS 4 兼容安卓、UA 里**同时带 Android**，所以鸿蒙判断必须排在 Android 之前，否则会误判成「Android / Chrome」给出错误步骤。
2. **新增两套步骤**
   - 鸿蒙 / 华为浏览器：用自带浏览器打开 → 底部「三横线」菜单（或地址栏右侧「⋮」）→ **添加到桌面**；菜单里没有就试「分享 → 添加到桌面」或长按页面。
   - 鸿蒙 / 微信：右上角「…」→ 在浏览器打开 → 再按上面的步骤。
3. **纯血鸿蒙（HarmonyOS NEXT）说明**：给出的是「网页快捷方式」，点图标用浏览器打开、顶部可能仍显示地址栏；功能/数据/登录态与浏览器里完全一致；想要更像 App 可在浏览器菜单开「全屏浏览 / 无工具栏」。
4. **「我的设备」下拉**：设置 → 外观 → 装到手机桌面 里新增，可手动切换 8 种设备/浏览器组合（自动识别、鸿蒙、鸿蒙微信、iPhone Safari、iPhone 其他、Android Chrome、Android 微信、电脑）。识别不准时手动选即可，选择会记住。

### 鸿蒙用户现在怎么做（1 分钟）
1. 用手机自带的**浏览器**（华为浏览器）打开 https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy
2. 点底部工具栏的**「三横线」菜单**（部分版本在右下角，或地址栏右侧「⋮」）
3. 选 **「添加到桌面」** → 确认
4. 回到桌面，点新图标即可直接进入；同一台设备 **7 天内免密**，数据与电脑端共用云端同一份

> 若你用的是微信打开链接：右上角「…」→「在浏览器打开」，再走上面 2-4 步。

### 质量保障
- `node --check` 通过；新增 `_tools/_smoke_v53.js` **36/36** 通过（含鸿蒙 4 / 纯血鸿蒙 / 鸿蒙微信 UA 识别、手动切换、步骤文案、渲染、源码断言）。
- 回归全绿：v52 36/36、v51 33/33、v49 30/30、v48 41/41、v42 13/13。
- 线上静态 **/30/**，关键标识全部命中。
- 附加修复：下拉同步时未选中项曾写入空的 `selected` 属性（HTML 布尔属性，存在即视为选中），已改为 `removeAttribute`，避免真实浏览器里选中项错乱。

---

## v55 · 全屏 App 外壳页（2026-09-17 上线，链接部分网络不可达，已降级）⚠️

> **核心结论：这个方案的链接在你的鸿蒙手机上打不开，不要依赖它。日常装桌面请用稳定链接 `https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy`。**

### 做了什么
- 新建独立目录 `app-shell/`：外壳页 `index.html`（全屏 iframe 嵌套工作台分享链接 + 加载动画 + 12s 兜底按钮 + postMessage 名称同步）+ `manifest.webmanifest` + 三张图标（180/192/512，由 v52 图标 base64 解码）。
- 经 sites 发布为独立应用，链接 `https://richangji-shell.app.workbuddy.host/`。

### ⚠️ 为什么打不开（已排查）
- **服务端完全正常**：HTTP 200、证书有效、时延 <1s、无冷启动、无 X-Frame-Options（可被嵌套）。
- **你的手机网络不通**：你鸿蒙手机所在网络到 `app.workbuddy.host` 这个 CDN 节点**连接超时**——这是网络层问题，优化页面/重发都解决不了。
- **对照证据**：你平时能正常打开 `workbuddy.link` 体系，说明那个域名在你手机上是通的；而 `app.workbuddy.host` 通不到。

### 当前做法
1. **删掉桌面上那个打不开的 shell 图标**；改用稳定链接重装：
   `https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy`
   （华为浏览器打开 → 底部「三横线」菜单 →「添加到桌面」→ 确认）
2. 桌面图标名由平台外壳页控制，工作台内「外观/名称」改不了它——这是托管架构硬限制，不是 bug。
3. `richangji-shell.app.workbuddy.host` 暂保留但**不依赖**；换个网络（如切 4G）也许能开，但作为日常入口不可靠。
4. 此方案**未改动工作台本体** `life-all-in-one.html`，风险隔离；外壳代码在 `app-shell/` 目录，将来要换更稳的托管可在此改。
