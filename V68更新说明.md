# v68 修复说明（已上线）

线上链接：https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy （版本 /51/）

本次一并修复你反馈的三个问题。

## 1. 物品「使用频率」没有同步
- 原因：使用次数统计一直只存在手机本机，从来没有随设置项上传云端（代码里漏了这个字段，属于历史遗留缺陷）。
- 修复：使用频率纳入云端同步。电脑上累计的「−」使用记录会在下次打开工作台时自动推上云，手机拉取后即可看到同一份统计；两边都记使用次数时按「取较大值」合并，不会互相覆盖、不会翻倍。

## 2. 采购清单的脏数据
- 查证：云端购物清单 7 条全部是真实记录，没有脏行。手机上那条「燕麦奶 2 盒 · 无糖款」是内置示例数据——之前手机浏览器清过一次本地缓存，应用重新初始化时把示例又种了回来（示例数据只存本机、永不进云端）。
- 修复：
  - 打开工作台并同步成功后，只要是已经在正常使用的设备（云端有你的真实数据、或执行过清空），所有示例记录、示例书影音、示例打卡会自动整批清除，无需手动删；
  - 每日备份快照不再包含示例数据，旧快照恢复时也会自动过滤，示例不会再被「恢复」回来。
- 手机上刷新一次页面，燕麦奶会自动消失，「本月买到」统计会从 8 恢复为真实的 7。

## 3. 时光档案当日时间线乱序
- 原因：当日时间线按记录添加顺序排列，不是按时刻。
- 修复：当日时间线改为按时刻先后升序排列（上午 → 下午 → 晚上时刻未记录殿后），现在会显示为 上午2点 → 上午10点 → 下午3点。

## 验证
- 语法检查通过；新增 28 项冒烟测试全过；多用户（32 项）与每日备份恢复（41 项）历史回归全过；部署后线上 92 个关键标记全部命中。

手机上下拉刷新（或关掉页面重开）即可生效。
