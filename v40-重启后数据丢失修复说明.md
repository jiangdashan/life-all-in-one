# v40 — 重启后"删除/修改丢失"修复说明

> 发布链接：https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy （静态 /14/）
> 请手机和电脑都 **Ctrl+F5 强制刷新** 确认地址栏版本为 /14/ 后再用。

## 你遇到的三个现象，根因是一类的

电脑重启（= 浏览器关闭再打开）后，工作台会重新从云端拉取数据并和本地合并。问题出在**"本地做的删除/修改没可靠地写回云端，而拉取时云端旧数据又把本地覆盖/拉回"**。具体分三处：

### 1. 习惯复活（看书 / 冥想 / 睡觉）
这三个是**内置习惯**。删除内置习惯时，程序把它的 key 记进 `hiddenHabitKeys`（隐藏名单）。
但合并函数 `mergeRemoteHabits` 只用名字名单 `deletedHabitNames` 去拦云端习惯定义，**完全没用 `hiddenHabitKeys`**，于是每次同步/重启，云端保存的这三个习惯定义都会被重新加回列表 → 复活。
（这个 bug 与网络无关，**必然发生**。）

### 2. 待买复活（燕麦奶）
删除记录时，程序调用一次"删除云端"的异步请求，是**发出后不管、不确认也不重试**。如果这条请求当时没成功上云（网络抖动、或页面在请求完成前被重启关闭），云端仍保留该记录，`mergeShopping` 下次又会从云端拉回来 → 复活。

### 3. 计划吃什么被清空
"计划吃什么"是设置类数据。同步逻辑在判定"云端比本地新"时，会**用云端整份设置直接覆盖本地**。只要云端这份设置里饮食计划是空的（比如你录入后没成功上云），本地刚录入的计划就会被清掉。

---

## 修复方式（已上线 /14/）

| 问题 | 修复 |
|------|------|
| 习惯复活 | `mergeRemoteHabits` 现在同时用 `hiddenHabitKeys`（按 key）过滤：被隐藏的内置习惯既不会从云端定义被加回，也会从本地列表移除 |
| 计划吃清空 | `dietPlans` / `weeklyPlan` 改为**并集去重合并**，云端为空时绝不会清空本地已录入的数据；本地未上云的修改始终保留 |
| 待买复活 | 删除记录时把待删项的云端 ID 记入 `pendingDeletes` 队列；重启或每次同步后自动**重试云端删除**，直到云端真正删除，不再"删了又拉回" |

---

## 怎么验证 / 怎么用

1. **强刷**：手机、电脑都 Ctrl+F5，确认地址栏是 `/14/`。
2. **习惯**：强刷后看书/冥想/睡觉会自动消失（之前的隐藏标记已在本地）。如仍出现，重新删一次即可彻底生效并同步到其它设备。
3. **待买**：若燕麦奶又出现了（云端残留），再删一次——现在删除会可靠同步到云端，不再复活。
4. **计划吃**：重新录入"计划吃什么"，刷新/重启后不再被清空。

> 云端原始数据没有受损，这次是客户端同步逻辑问题，不是你操作丢失。以后删除/修改都会可靠上云，重启不会丢。

## 测试
新增冒烟测试 `_smoke_v40_sync_safety.js` **10/10** 通过（习惯不复活、计划吃不空、删除入队去重）；
回归 v39 26 / v38 8 / v37 17 / v36 26 / v35 11 / v34 7 / v32 20，**共 145 项全绿**。
改动前已备份 `backups/life-all-in-one-pre-v40_20260914_114410.html`。
