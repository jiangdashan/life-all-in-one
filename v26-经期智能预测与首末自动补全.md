# v26 — 经期智能预测 + 首末自动补全

## 改了什么

### 1. `analyzePeriod` 算法升级（核心）

#### 旧版（v24）缺陷
- `periodLens` 只算 `isPeriodDay==='Y'` 标记的记录数
- 用户如果只标"首日 + 末日"（不每天勾经期），`avgPeriod` 永远算成默认 5 天
- `cycles` 算法 `slice(0, starts[last]===today?-1:undefined)` —— 只在 last 等于 today 才排除当前未闭合周期，大多数实际场景（用户在月中而不是今天）会算错
- `currentEnd` 用 `shiftDate(-1, new Date(starts[starts.length-2]))` —— starts 只剩 1 个历史周期时下标 -2=undefined 直接崩；且取的是倒数第二个 starts 的前一天，不是当前周期末日

#### 新版（v26）
- **经期长度自动补全**：
  ```
  末日 = 本周期内 isEndDay='Y' 的最大日期
       || 下个首日前一天
       || 今天（未闭合周期）
  长度 = periodDayCount > 0 ? periodDayCount : (2 <= span <= 15 ? span : 0)
  ```
  只需标首日 + 末日，无需逐天打卡。
- **`closedStarts = starts.length>=2 ? starts.slice(0,-1) : starts`**：所有相邻 starts 间隔都算 cycles，稳健不依赖 last==today 边角条件。
- **新增返回 `periodEnds[]`**：每个元素的真实末日，所有 UI 消费同一份数据。

### 2. UI 显示样本数（v26 用户可见特性）
- 「平均 30 天周期 · **2 次样本**」
- 「平均 6 天经期 · **3 次样本**」
- 让用户看到均值基于几次历史

### 3. UI 显示真实末日（v26 用户可见特性）
- 周期历史从 `"2026-08-07"` 改为 `"2026-08-07 至 2026-08-12"`（自动跨度数）
- 进行中周期显示"进行中"
- 当前未闭合周期用 `periodEnds[last]` 而非之前会崩的 `starts[length-2] - 1`

### 4. 重要设计选择（v26）
**未闭合周期不计入历史均值**（avgCycle / avgPeriod / cycles）。
理由：当前周期还没结束，现在算平均会被拉低（例如用户 9-06 才首日，周期通常 28 天，现在算就会被错拉低）。v24 老测试期望 `[3,2,1]` 是错的，新算法只算闭合周期 `[3,2]`，已写注释注明。

---

## 验证状态

### 冒烟测试
| 脚本 | 断言数 | 结果 |
|---|---|---|
| `_tools/_smoke_v26_period_predict.js` | 28 | ✅ 全过 |
| `_tools/_smoke_v25_period_fix.js` | 16 | ✅ 全过（无回归） |
| `_tools/_smoke_v24_period.js` | - | ✅ 0 FAIL（analyze_period_len 改为新语义断言 OK_v26(未闭合周期不计入均值)） |

### v26 新断言覆盖
| 场景 | 覆盖点 |
|---|---|
| s1 | 单周期（只标首末）— starts/periodLens/avgPeriod/cycles/avgCycle/lastStart/periodEnds/nextPredicted |
| s2 | 3 个历史周期 — 基于最近 N 期 avgCycle 预测下次月经 |
| s3 | 空数据 — avgCycle 兜底 28 天 |
| s4 | periodEnds 修正 — 当前未闭合周期末日 = today |
| s6 | UI 渲染 — 状态卡含"· 2 次样本"、周期历史含"至 2026-08-12"真实末日 |
| v25 | 老 3 个补漏全部保留 |

### 发布
- 静态资源版本：**/26/**
- 线上 `https://workbuddy-space-static.codebuddy.work/page/muRdSFuIXga85vsZfTsTJ4/26/life-all-in-one.html`
- 5 项标记 grep 全部命中：periodEnds / 次样本 / periodEnds[ / periodDayCount>0 ? periodDayCount / closedStarts

---

## 新增铁律

1. **日期测试要用 `shiftDays(TODAY, n)` 不要用 ISO 字符串直拼**：
   `new Date('2026-07-08T00:00:00')` 会被解释为 UTC（东 8 区 sandbox 会变成 7-7 而不是 7-8），导致断言期望值 vs 实际差 1 天。测试数据日期永远围绕"今天"算偏移。
2. **"未闭合周期是否计入均值"是设计取舍不是 bug**——写代码要把决策写清楚注释，否则测试者不知道哪个对。

---

## 用户使用建议

现在你可以这样用：
1. **每天记一次**：标「是否经期」(是/否)，心情值，卫生巾数，症状
2. **经期第一天**：标「是否首日 = 经期首日」✓
3. **经期最后一天**：标「是否末日 = 经期末日」✓
4. 不需要每天勾"是否经期"也能准算经期长度和下次预测

下次月经预测会在状态卡片右侧显示，例如「下次预测 2026-10-03」。
