# v36b 数据体检说明 — Web 端"数据丢失"排查

## 核心结论：云端数据一条都没丢

我对云端全部 10 张表做了两次全量核对（11:35 与 12:02），结果完全一致：

记账 16 条 · 习惯 58 条 · 日程 14 条 · 健身 5 条 · 待买 2 条 · 书影音 65 条 · 饮食 18 条 · 物品存储 51 条 · 心情 2 条

没有任何删除发生。而且设置数据今晨 06:06 还成功同步过，说明同步链路是通的。

## 为什么页面上看不到？

页面只显示「属于当前登录身份」的记录。你看到"部分模块不完整/书影音全丢"，说明**你当前打开的那个窗口，登录身份和数据的归属身份对不上**——常见于：在某个新窗口（客户端资料库地址、对话里的预览窗口）打开时被当成"新设备"，设了一个不同的密码。

另外请注意：**对话里打开的那个预览窗口（地址 127.0.0.1:53286）没有云端连接**，在里面看到/记录的一切都不是真实数据，请不要在那个窗口里使用应用。

## 怎么办：用「数据体检」一键定位（已上线）

1. 打开正式链接并 **Ctrl+F5 强制刷新**：
   https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy
2. 输入你的访问密码登录。
3. 点左下角「自定义」打开设置面板，拉到最下方新增的「**数据体检**」，点「开始数据体检」。
4. 体检会列出每张表「云端总数 / 当前身份可见 / 本机条数」和你的身份编号：

   - 若显示 **⚠ 云端有数据但当前身份看不到** → 点「把云端数据归属到当前身份」，云端所有记录会立即改为归属你当前的身份（只改归属，不删不改任何内容），数据全部回来。
   - 若显示 **✅ 云端数据完整且匹配** → 数据都在，只是展示/网络问题，刷新或点顶栏「同步」即可。

5. 把体检表格的结果告诉我（照抄几行即可），我可以进一步精确定位。

## 关于"今天新记的记录"

云端查不到任何今天（9月7日）新增的记录。如果今天记过东西，很可能记在了"无云端连接的窗口"（预览窗口）或一个身份不对的窗口里——确认一下是在哪个窗口记的，体检结果出来后我们一起核对。

## 本次更新还修复了

- 经期清空标记此前会被身份过滤漏读（跨设备经期清空未真正生效），已改为全量读取。
- 测试：v36 冒烟 19/19，v32/v34/v35 回归全过。
