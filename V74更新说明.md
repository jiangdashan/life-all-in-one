# v74 更新说明（线上版本 /57/）

两件事其实是一个 bug 的两面。

---

## 一、习惯健康页顶部为什么会出现「代码」

### 那串代码是什么

是**倒数日功能的样式代码**（CSS），本该藏在 `<style>` 标签里生效，结果被放到了标签外面，浏览器就把它们当成普通文字显示出来了 —— 所以你在习惯健康页最上面看到 `.cd-sub{...}`、`.cd-hero{...}`、`@media(max-width:640px){...}` 这一串。

### 为什么会放错位置

上一版（v73）加倒数日时，脚本要"在样式表末尾追加一段样式"，它找的标记是 `</style>`。但这个页面里**不止一个样式表**——习惯健康这个分页自己也带了一个内联样式表，那个标签先被匹配到，新样式就被追加在了它的闭合标签**后面**，也就是标签外面。

顺带还留下了一个多余的 `</style>`。

### 已修

- 删掉了那段泄漏出来的文字和多余的闭合标签（共 1191 个字符）。
- 样式改回放到页面唯一的**主样式表**里，并加了一行注释标注，防止以后再插错。
- 加了检查：习惯健康页的可见内容里不允许再出现任何样式代码。

---

## 二、倒数日页面重做

因为样式一直没生效，倒数日此前等于"裸奔"，所以难看。修好位置的同时把版式重做了一遍，风格跟其他页保持一致（无彩色装饰、无阴影、无荧光）。

### 顶部卡片

- **左边主卡**：最近的一个日子 —— 名称、38px 的大数字天数、下面的具体日期和农历说明。
  - 正好是当天时，显示"就是今天"，而不是"还有 0 天"。
- **右边小卡两枚**：接下来最近的两个日子，一行一个，右侧带天数。
- 手机上（窄屏）自动变成上下单栏。

### 清单

- 每一行：左边是天数（数字用等宽对齐，不会随着位数变化左右跳动），中间是名称 + 日期说明，右边是"公历/农历"标签，自定义项右侧有删除按钮。
- **7 天以内**的日子：边框和数字转为红色（`-red`），一眼能认出快到了。
- **当天**：显示"今天"，不再显示 0 天。
- 名称过长自动省略号，不会撑破布局。

### 没动的部分

倒数日的算法、内置节日清单、农历换算、自定义添加与云端同步 —— 全部保持原样，只改样式和结构。

---

## 验证

| 项目 | 结果 |
| --- | --- |
| 语法检查 | 通过 |
| 新增测试（v74） | 43 项全过 |
| 历史回归 | v57 27、v63 32、v65 41、v67 22、v68 28、v69 39、v70 21、v71 28、v72 39、v73 56 —— 全部 0 失败 |
| 线上标记断言 | MISS 0（版本 /57/） |

其中 `_smoke_v73` 有一条断言写死了旧的顶部结构（`cd-hero-days`），已按新结构更新为 `cd-hero-count`。顺带发现一个测试陷阱：内置节日可能比测试里构造的自定义日子更近（9 月 22 日时国庆只剩 9 天，而构造的是 10 天），所以断言不能写死具体天数。

---

## 你需要做的

手机上下拉刷新一次即可。习惯健康页顶部那段代码会消失，倒数日恢复正常版式。
