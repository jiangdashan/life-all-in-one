# 生活全能工作台 · v20 变更说明

发布日期：2026-09-04

本次针对「每日饮食」与「心情打卡」两处体验做了 4 项调整，已部署上线。

## 在线链接
https://workbuddy.link/p/muRdSFuIXga85vsZfTsTJ4

> 若仍看到旧内容，请**强制刷新**（桌面 Ctrl+F5 / 移动端清除缓存）一次。

---

## 1. 每日饮食

### ① 默认只展开「今天」，其他日期收起
- 饮食记录里的日期分组，现在**只自动展开今天**那一组（日期头显示为「今天」）。
- 其余历史日期的记录默认收起成一条标题，点开才看得到，避免页面被历史记录撑得太长。

### ② 饮食记录区带独立内滚动条
- 「饮食记录」列表本身是一个独立、有上下内滚动条的容器，滚动只发生在记录区里，不会拖拽到上方的记录表单 / 饮食计划 / AI 推荐区域。

---

## 2. 心情打卡

### ③ 删掉心情月历底部「强度」图例
- 心情月历下方那排「强度 1–5 级」的说明图例已移除，月历更清爽。

### ④ 心情轨迹回显「今日感受」
- 心情轨迹（最近的每一天）每行现在会**回显当天填写的「感受」文字**（日期下方小字）。
- 只有当时填了感受的记录才会显示；没填的仍只显示表情与日期。
- 界面继续遵循「心情只显示 emoji、不显示文字标签」的既定风格——感受是补充文字说明，不影响表情主展示。

---

## 改动文件
- `life-all-in-one.html`（本地已更新）
- 已通过 import_html + publish_page 发布，线上静态资源版本 `/20/`

## 验证
- JS 语法校验通过（node --check）
- DOM 冒烟自测通过：今天分组默认展开、旧日期分组默认收起；月历强度图例元素已删除；心情轨迹行正确显示感受文字
