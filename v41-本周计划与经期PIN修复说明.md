# 生活全能工作台 v41 更新说明（已部署 · 静态 /16/）

> 本轮（v41）实现你提出的 7 项需求。**已部署**到 `https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy`（静态版本 /16/）。
> 打开后请 **Ctrl+F5** 硬刷新，确保加载新版本。

## 1. 减脂健身「本周计划」改造 ✅（含你的补充：每周连计划项一起清空）
- **不再写死数据**：移除 `DEFAULT_PLAN` 硬编码种子，首次打开默认**空计划**。
- **每周清空（含计划项）**：`rolloverWeeklyPlan()` 在每周一跨周时，先把上一周计划**归档到「历史计划」**，再把**整个本周计划清空**（不只是重置打卡状态）——即每周从空白重新开始，需自行新增。
- **历史计划查看入口**：健身视图「本周计划」面板新增「**历史计划**」按钮，折叠查看过往每周的打卡情况（保留最近 12 周）。
- `weeklyPlanHistory` / `weeklyPlanWeekStart` 已纳入设置快照与云端同步，跨设备可见。

## 2. 目标设置恢复实际数据 ✅（已写回云端）
- **根因**：`applySettingsSync` 此前用 `Object.assign` 拿云端副本整体覆盖本地 `fitnessProfile`，旧/错的身高体重年龄会被旧副本悄悄抹掉。
- **已修复（防复发）**：`fitnessProfile` 改为**末写胜出**——仅当云端 `fitnessProfileUpdatedAt` 严格大于本地时才覆盖；本地每次修改都会刷新时间戳。
- **已恢复实际值**：经排查云端发现你的真实设置记录（被后来的默认副本覆盖），已新建一条带最新时间戳的设置记录，把你原本的数值写回：
  - 身高 **158cm**、起始体重 **54.9kg**、年龄 **32**、目标体重 **50kg**、活动系数 **1.55**（目标体重/活动系数取自你之前实际保存的云端记录）。
  - 该记录 `updatedAt` 为当前最新，因此会在下次同步时胜出；你打开新版本后即可看到正确数值。
  - 注意：你本地设备若此前已被覆盖成错误值，**打开新版本后会从云端拉回正确值**（无需手动改）。

## 3. 日程统筹「智能清单」内置滚动条 ✅
- `#plannerList` 增加 `max-height` + `overflow-y:auto` + 细滚动条，长清单不再撑爆页面（移动端同样生效）。

## 4. 书影音清单编辑功能 ✅
- 每张卡片新增「编辑」按钮（铅笔图标），复用新增表单作编辑载体，`_editingMediaId` 标记编辑态，保存时回写名称/类型/状态/评分/短评/日期并同步云端；提交按钮文案在编辑时变为「保存修改」。

## 5. 物品存储「库存总览」搜索 ✅
- 总览面板新增搜索框（按名称/分类/位置/备注实时过滤，大小写不敏感），带「清除」按钮，结果行仍可增减数量。

## 6. 心情打卡隐藏三项指标 ✅
- 「今日心情 / 本周平均 / 已记录天数」三个指标卡默认 `hidden`，界面更干净。

## 7. 经期 PIN 错误 / 取消 PIN 后历史是否展示 ✅
- **取消 PIN 后历史照常展示**：经期记录存在 `state.records`，PIN 只用来锁「锁屏面板」，不碰任何记录数据。清除后 `renderPeriod()` 直接渲染全部历史。
- **为什么提示 PIN 错误**：旧代码 PIN 的盐值（salt）**未纳入同步**，校验依赖本地 salt，一旦丢失就对不上哈希。已修复——`periodPinHash` / `periodPinSalt` 现在纳入同步载荷，PIN 跨设备一致。
- **新增「忘记 PIN？清除并查看历史」按钮**（锁屏页，无需旧 PIN 即可清除），解决「输不对、又够不到清除按钮」的死锁。

---

## 部署与验证
- 重导入 `life-all-in-one.html` 并发布，关联全部 11 张表（money/habit/plan/fitness/shopping/media/diet/storage/mood/period/meta）。
- 静态版本：**/16/**（中间 /15/ 因一处 `weeklyPlan` 清空代码未生效已作废重发）。
- 已逐项核对线上静态文件含全部 v41 标记：`rolloverWeeklyPlan`、每周清空 `weeklyPlan=[]`、书影音 `i-edit`、库存搜索 `storageOvSearch`、心情 `metric-row hidden`、经期「忘记 PIN」入口，以及 `fitnessProfile` 默认值 `158/54.9/32`。
- 已直接修正云端 `appSettings` 记录为你的真实数值（最新 `updatedAt`，确保同步时胜出）。

> 提示：源 HTML 仅用于部署，不要在本机 127.0.0.1 直接打开——那里无法桥接云端，会产生孤立数据。
