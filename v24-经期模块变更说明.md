# v24 变更说明：经期记录模块上线

**发布时间**：2026-09-06 上午  
**在线链接**：https://workbuddy.link/p/muRdSFuIXga85vsZfTsTJ4

---

## 一、新增功能

侧边栏新增「经期」入口（图标为月牙花瓣），点击进入经期记录视图。

### 1. 4 位 PIN 锁（默认开启）
- **首次访问**：「设置 PIN」弹层，输入两次 4 位数字确认
- **再次访问**：输入正确 PIN 后整次会话可查看数据（sessionStorage 标记解锁，刷新页面需重新输入）
- **PIN 跨设备同步**：PIN 哈希值（SHA-256 + 随机盐）会同步到云端「同步标记表」，换个设备打开时输入同一个 PIN 即可
- 「重设 PIN / 清除 PIN」在「PIN 管理」面板里

### 2. 今日身体状况日志
**状态**（单选）：
- 🩸 经期首日 / ● 经期中 / ○ 经期末日

**分泌物**（单选）：无 / 粘稠 / 水样 / 蛋清样

**症状**（可多选 chips）：疲倦、失眠、乳房胀痛、腹痛、浮肿、心情低落、亢奋、食欲增加、头痛、腰酸

**心情**：0-10 滑块 + 文字标签

**出血量**：0-5

**用品计数**（+- 按钮）：卫生巾数量、安睡裤数量

**备注**：自由文本

### 3. 当前阶段状态卡
顶部显示：
- 当前所处阶段（月经期 / 卵泡期 / 排卵期 / 黄体期）
- 阶段 emoji（🩸 🌱 🌸 🌙）
- 阶段详细提示（每阶段内置文案：典型症状 + 情绪 + 行为建议）
- 平均周期天数、平均经期天数、**下次月经预测日期**、**排卵日预测**

### 4. 4 阶段可视化卡片
四张卡片横向排列（移动端 2×2），当前阶段高亮标注「← 当前」。

### 5. 周期历史列表
倒序列出过去每次经期：
- 开始日期 / 至结束日期（或「进行中」）
- 经期天数 + 周期天数（已完成周期）
- 本次经期使用的卫生巾+安睡裤总片数

### 6. 用品用量统计
- 本次经期：累计卫生巾 + 安睡裤
- 累计：所有经期卫生巾 + 安睡裤
- 本次经期天数

### 7. 各阶段情绪均值
按月经期/卵泡期/排卵期/黄体期四个阶段，分别算这段时间内所有心情自评的平均分（0-10），卡片显示：阶段 emoji + 平均分 + 该阶段样本数。

---

## 二、技术说明

### 云端
- 新建表 **DB_PERIOD**（id: `rp51GH61XV6eq9fl4smboC`），12 字段：日期 / 是否经期 / 是否经期首日 / 是否经期末日 / 分泌物 / 感受 / 心情值 / 心情标签 / 卫生巾数量 / 安睡裤数量 / 出血量 / 备注
- 数据同步：web 端新增记录后立即上云，手机端刷新即可看到
- 备份脚本 `_backup_remote.py` + `_restore_remote.py` 已加 period 表

### 周期分析算法
- `starts` = 所有 isStartDay='Y' 的记录日期排序
- `cycles` = 相邻 starts 间隔天数（**排除「当前未完成周期」**，避免把正在进行的周期算进平均）
- `avgCycle` = 最近 3 次完整周期的均值；只有 1 次记录时默认 28
- `periodLens` = 每段经期 isPeriodDay='Y' 的连续天数
- 阶段判定（相对 lastStart）：
  - dayOfCycle ≤ avgPeriod → **月经期**
  - dayOfCycle ≤ avgCycle-14-2 → **卵泡期**
  - dayOfCycle ± 2 around (avgCycle-14) → **排卵期**
  - 其余 → **黄体期**
- 预测：nextPredicted = lastStart + avgCycle；排卵日 = lastStart + avgCycle - 14

---

## 三、隐私说明

- 经期数据已和记账/习惯/健身一样存储在云端（便于多设备同步）
- 但因数据敏感，**PIN 锁是默认保护**：所有用户访问经期视图前都必须输入 PIN
- 页面是公开链接（`workbuddy.link/p/...`），任何拿到链接的人打开都需要 PIN 才能看
- PIN 哈希 + 盐通过「同步标记表」自动跨设备同步，但**不会以明文上云**
- 忘记 PIN：在另一设备上用「重设 PIN」即可（云端不能反查明文）

---

## 四、验证

- 30+ 冒烟断言全过：PIN 设置 / 锁定 / 错误 PIN / 正确 PIN / 状态按钮 / 分泌物芯片 / 计数按钮 / addRecord / 记录渲染 / 周期历史 / 用品统计 / 阶段分析 / 情绪统计 / analyzePeriod 多周期 28 天 / periodLens [3,2,1] / mergePeriod 字段映射 / 移动端导航
- 节点语法检查通过；线上发布版本 `/24/` grep 验证全部 marker 存在
- 当前已备份 186 条记录到 `D:/workbuddyProjects/工作台3/backups/2026-09-06_v24/`

---

## 五、上线后的使用

1. 打开 https://workbuddy.link/p/muRdSFuIXga85vsZfTsTJ4
2. 强制刷新一次（Ctrl+F5 / 清缓存）以加载新版静态资源
3. 侧边栏点击「经期」（月牙花瓣图标）
4. 首次设置 4 位 PIN；之后每次访问经期视图都要输一次 PIN

**至少录入 1 次「经期首日」+ 「经期末日」**后，状态卡才会显示阶段、预测、周期历史。