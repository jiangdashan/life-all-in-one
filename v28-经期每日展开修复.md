# v28 — 经期周期每日展开 + 每日详情卡片

> **本次未发布，仅本地完成 + 完整冒烟 + 全部回归通过。等用户点头后再发布。**

## 需求（用户原话）

> 要支持一段周期的每一天单独记录，也要支持当我只记录这个周期首日和末日时自动补齐中间的每天，最终展示在历史时不许只是首日和末日两天

## 解决方式：展示层展开，不创建新 record

**`analyzePeriod()` 新增返回值 `cyclesExpanded`** —— 每个周期一个对象，含 `days: [{date, source, record}]`：
- `source` ∈ `{'start' | 'mid-manual' | 'end' | 'mid-auto'}`
  - `start` — 该周期首日
  - `end` — 该周期末日
  - `mid-manual` — 用户手动打了「经期中」的天
  - `mid-auto` — 系统自动补齐（用户没记这天，但通过首末标记推断）
- **不创建任何新 record** —— 只在派生数据里展开，避免 v27 那种同步翻倍

**`renderPeriod()` 周期历史改为可展开卡片**：
- 默认显示总览：首日 → 末日 / N 天经期（自动补 X 天 · 手记 Y 天）/ Z 片用品
- 点击右下方 ▾ 按钮 → 展开每天详情列表（首日/经期中/末日/经期中（自动补））

## 用户提的所有边界都覆盖

| 场景 | 实现 | 测试 |
|---|---|---|
| (A) 每天单独打卡，每条 record 标 isPeriodDay='Y' | days 全部为 mid-manual | v28_s3 ✅ |
| (B) 只标首日+末日，中间不打卡 | 中间天自动为 mid-auto | v28_s1, s4 ✅ |
| (C) 首日+中间打几天+末日 | 中间打的为 mid-manual，没打的为 mid-auto | v28_s2 ✅ |
| (D) 当前周期（未标末日）→ 展开到 today | isLastCycle 分支处理 | v28_s1, dirty ✅ |
| (E) 同日 3 次保存 = 1 条 record（v27 教训）| v27 合并逻辑未被破坏 | v27_s1 ✅ |
| (F) 脏数据 starts 去重（v27 教训）| 保留 Set 去重 | v27_s2 ✅ |
| (G) 历史周期漏末日不延展到 today | v28 修复：startDate fallback | dirty ✅ |
| (H) 末日超出 nextStart 自动矫正（v26 行为）| lastEndDay 边界夹断 | s4 ✅ |
| (I) 历史 3 周期平均预测下次 | avgCycle 计算不变 | s2 ✅ |
| (J) 折叠/展开切换 + aria-expanded | toggle-cycle handler | v28_s7 ✅ |
| (K) 加 addRecord 或 renderPeriod 不会污染 state.records | 派生数据分离 | v28_s8 ✅ |
| (L) 情绪统计只算手动日记（自动日不计分）| record 引用按真实数据 | v28_s9 ✅ |

## 冒烟测试结果

| 版本 | 断言数 | 通过 | 状态 |
|---|---|---|---|
| v28（新增 39 项） | 83 | 83 | ✅ |
| v27 回归 | 40 | 40 | ✅ |
| v26 回归 | 28 | 28 | ✅ |
| v25 回归 | 16 | 16 | ✅ |
| v24 回归 | ~20 | 0 FAIL | ✅ |

总计 **167 项断言全部通过**。

## UI 行为

打开经期模块，每个周期的历史卡片显示：

```
2026-09-03 → 至 2026-09-07                ▾
4 天经期（自动补 3 天）/ 30 天周期           0 片用品
```

点击 ▾：

```
2026-09-03   🩸 首日             -
2026-09-04   经期中（自动补）    -
2026-09-05   经期中（自动补）    -
2026-09-06   经期中（自动补）    -
2026-09-07   🩸 末日             -
```

颜色区分：
- 首日 / 末日：紫色（强调）
- 自动补的日：淡琥珀底，标注「（自动补）」
- 手记的日：纯文本，标注「经期中」

## 数据流（关键：不写 state.records）

```
用户保存 1 次首日  ─┐
用户保存 1 次末日  ─┴─→ state.records 增 2 条 record
                           ↓
                       analyzePeriod(records)
                           ↓
                   + cyclesExpanded（派生）
                           ↓
                       renderPeriod()
                           ↓
                       UI 显示完整展开
```

任何时候 state.records 数 = 用户真实保存次数。无自动增长。

## 文件改动

- `life-all-in-one.html`（v28 本地版）
  - `analyzePeriod` 多返回 `cyclesExpanded` 数组
  - `endDate` 计算逻辑加 `isLastCycle` 分支
  - `renderPeriod` 周期历史改为可展开
  - `bindEvents` 加 `data-action="toggle-cycle"` 处理
  - CSS 加 `.period-cycle-days` / `.period-day-row` / `.expand-btn` / `.expand-btn.expanded`

## 之前出现过的问题

按用户原话"之前出现过的问题不许再出现"：

| 版本 | 问题 | v28 处理 |
|---|---|---|
| v24 | periodForm submit 没绑 → 不能保存 | 保留 v25 submit binding |
| v24 | mood slider 数字不实时 | 保留 v25 input listener |
| v24 | TYPE_META 没加 period → 档案视图显示"生活" | 保留 v25 TYPE_META |
| v26 | periodLens 不用末日标记 | 保留 v26 算法 |
| v27 | 同日多次保存 = 多条 record → starts 重复 | 保留 v27 合并逻辑 |
| v27 | 合并时空字符串覆盖有效字段 | 保留 v27 pickNonEmpty |
| **新发现** | 历史周期漏末日 → 循环到 today 显示 16 天"自动补" | **v28 新增：startDate fallback** |

## 不发布

按用户原话："**确认完全满足我的需求且无问题后再同步给我**" —— **本次未发布**。等用户在浏览器强制刷新验证本地版（或后续发布）后再 `import_html + publish_page`。
