# 生活全能工作台 · 修复报告（v14）

## 问题
web 端输入数据保存后，有一部分数据没有同步到移动端。

## 排查过程

先查远程 10 张表的真实数据（不靠猜）：

| 模块 | 远程条数 | 状态 |
|---|---|---|
| 记账 | 9 | 正常 |
| 书影音 | 29 | 正常 |
| 习惯打卡 | 7 | 正常 |
| 日程 | 5 | 正常 |
| 健身 | 5 | 正常 |
| 饮食 | 2 | 正常 |
| 待买 / 物品存储 / 心情 | 0 | 空 |

用 `get_database_schema.py` 核对三张空表的字段类型，与 push 代码**逐一比对完全匹配** —— 排除了字段类型问题。

## 真正的根因：容错设计缺陷（不是字段问题）

1. **单次失败 → 全局永久离线（致命）**
   `dbAdd` / `dbUpdate` / `dbDelete` 的 `.catch` 里都调用了 `goLocalOnly()`。只要任何一次写入、更新或删除失败（哪怕只是网络抖了一下），`LOCAL_ONLY` 就被置为 true，此后**所有 push 全部静默 return**，数据只存进浏览器本地、再也不上云。这就是「web 端存了、移动端看不到」。

2. **没有任何补传重试机制**
   push 只在记录创建时调用一次。如果这次失败（或被离线状态拦截），这条记录的 `remoteId` 就永远为空，**再也没有第二次机会**。整个同步只有「远程 → 本地」一个方向，缺了「本地 → 远程」。

3. **remoteId 提取不够健壮**
   只兜底了 `result.id || result._id`，而 SDK 契约并未明说 `addRecord` 返回的主键字段名。

## 修复内容

| 改动 | 说明 |
|---|---|
| 移除 3 处 `goLocalOnly()` | 单次失败改为只记日志，让记录保持「未同步」状态；离线判定只用于 SDK 环境本身不可用。现在全文件 `goLocalOnly` **零调用点** |
| 新增 `syncPendingRecords()` | init 拉完远程后，把本地所有未同步数据重推一次：records（按 type 分发 7 种 push）、书影音、习惯打卡（按 `habitRemoteIndex` 判断远程缺失） |
| 退避保护 | 记录 `syncTriedAt`，60 秒内不重复推，避免失败时反复刷新导致远程数据翻倍 |
| remoteId 提取加固 | `result.id` / `_id` / `record_id` / `recordId` 四种字段名兜底 |
| init 挂载 | 补传后延迟 2.5 秒统一落盘 + 重渲染，并提示「已补传 N 条未同步数据到云端」 |

## 验证

- JS 语法校验通过
- 提取 `syncPendingRecords` 真实运行 6 个场景，全部通过：
  1. 本地有未同步记录 → 全部补传
  2. 已同步 / 示例数据 → 不补传（不重复推）
  3. 60 秒退避 → 刚试过的跳过，超时的重试
  4. 书影音 + 习惯打卡补传
  5. 远程已有 → 不重复补传
  6. 离线模式 → 不补传
- 已发布，线上 v15 与本地完全一致

## 你需要做的

之前在 web 端「没同步上去」的数据**并没有丢**，它们还在 web 端浏览器的本地存储里。修复上线后：

1. 在 **web 端打开工作台并刷新一次** → 页面会自动把本地未同步的数据补传到云端（会提示「已补传 N 条未同步数据到云端」）
2. 再到 **移动端刷新** → 就能看到这些数据了
