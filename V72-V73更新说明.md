# 更新说明（v72 + v73）

线上版本 **/56/**，手机下拉刷新即可生效。

这次把你提的 7 条全部做完了。下面按你说的顺序说明，其中第 5 条（经期）是真正的数据算错，其余是功能补缺。

---

## 1. 日程按优先级排序

清单和四象限现在都按 **高 → 普通 → 低** 排列。

- 同一优先级里，**没做完的排在前面**，做完的靠后。
- 再同级时保留你原来的顺序，所以手动拖动排序的先后顺序在同一优先级内依然有效。

---

## 2. 时光档案支持按日期 / 周 / 月 / 年查看

原来的「当日 / 本周 / 本月 / 本年」只能看相对今天的，现在可以随意翻：

- **‹ › 按钮**按当前档位翻页：日 ±1 天、周 ±7 天、月 ±1 月、年 ±1 年。
- **日期输入框**可以直接挑任意一天跳过去。
- **回到今天**按钮只在离开今天时出现。
- 翻不过今天之后（不能预览未来）。
- 周历、月历、年汇总都跟着这个日期走，顶部标签会显示当前区间（例如「2026-09-21 ~ 2026-09-27」）。

---

## 3. 减脂本周计划自动清空并进历史

**原因**：同步时没有判断周次。只要另一台设备的云端快照还是上一周的计划，同步就会把它重新合并回来，所以新的一周看起来永远没清空。

**改法**：远端快照属于「更早的一周」时直接忽略；另外每次全量渲染兜底检查一次，跨周不用刷新页面。

上一周的计划会连同完成数一起进「历史计划」。

---

## 4. 倒数日

新模块，在左侧导航。

- **默认列出全部节日倒数**：元旦、劳动节、国庆、圣诞等 15 个公历节日；春节、元宵、端午、七夕、中秋、重阳、除夕等 10 个农历节日；还有母亲节、父亲节、感恩节（按「第几个星期几」算）。
- **自定义倒数日**：名称可选，支持**公历或农历**（农历可选闰月），可勾选「每年重复」或「只此一次」。
- 顶部显示最近的一个日子与剩余天数；清单按剩余天数升序；7 天内的会高亮。
- 自定义的可以删除；跨设备同步。

农历换算覆盖 1900–2100 年，春节、中秋、端午等节日的公历日期已多次核对无误。

---

## 5. 经期天数不再恒显示 2 天

**这是数据算错了，已修正。**

查了云端记录：你每个月只记了「经期首日」和「经期结束日」两条，中间几天没有逐日打卡。程序原本按「有记录的天数」算经期长度，于是每个历史周期都算成 2 天，平均值就永远显示 2 天。

**改法**：只要标了结束日，就用首日到结束日的跨度作为经期长度（中间靠自动补全，历史里会标「自动补 N 天」）。

实测同一份数据：平均经期由 **2 天 → 5 天**，8 月周期 5 天、9 月周期 6 天。经期阶段的判断也会跟着修正（以前第 3 天就被算作卵泡期了）。

---

## 6. 物品收纳位置归类

「存放位置」这个字段其实一直都有（表单里能填、云端也同步），缺的是归类查看。现在补上：

- 物品清单顶部多一行**位置筛选**：按位置带数量列出（如「厨房 2」「卫生间 1」「未标注位置 1」），点一下只看该位置。
- **库存总览**里新增「按收纳位置归类」区块：每个位置列出种类数、件数和前几件物品名，点一下直接筛到该位置。

---

## 7. 番茄钟时长累计

以前完成第二个番茄会把表单里的时长覆盖成 25。现在会累加：

- 连续完成三个 25 分钟番茄 → 表单时长 25 → 50 → 75。
- 面板显示「今天已完成 3 个番茄 · 专注 75 分钟 · 待记录 75 分钟」。
- 提交学习记录后自动清零；你也可以手动改时长框，之后从新数值继续累加。

---

## 验证情况

- 新增测试：v72 **39 项**、v73 **56 项**全部通过。
- 历史回归全绿：经期相关 216 项、v53 36、v57 27、v60 44、v63 32、v64 45、v65 41、v67 22、v68 28、v69 39、v70 21、v71 28、v72 39，共 **600+ 项，0 失败**。
- 部署后线上关键标记全部命中（MISS 0）。

### 顺带修的两个测试问题（不影响功能）

- `_smoke_v31` 把某一天写死当成「今天」，随着时间过去会误报 3 条。手工核对后确认是日期漂移而非代码回归，已改成动态基准日。
- `_smoke_v68` 有一条断言写的是「上午2点」，v70 之后 2 点改叫「凌晨2点」，断言已同步更新。
