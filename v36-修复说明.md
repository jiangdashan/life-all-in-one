# v36 修复说明 — 移动端与 Web 端数据不同步

## 诊断结论

先排查了云端数据：**所有记录都属于同一个身份（userId 一致），云端数据没有分裂、没有丢失**。问题出在"设备接入"这一侧，有两个根因：

1. **新设备登录页的陷阱（主因）**：手机第一次打开页面时，由于手机本地没有密码记录，页面显示的是「**设置访问密码**」。如果在手机上随手设置了一个和电脑**不一样**的密码，系统会派生出另一个身份 → 云端数据按身份过滤 → 手机上几乎什么都看不到（或只看到手机自己新记的）。
2. **旧链接隐患（次因）**：之前分享过的旧链接（muRdS… 节点）上还挂着 v29 老版本，没有同步机制。手机如果打开的是旧链接，在上面记的内容电脑端永远看不到。

## 修复内容（v36）

1. **新设备防呆**：手机首次打开时，页面会先探测云端是否已有数据——
   - 云端已有数据 → 登录页直接显示「**输入访问密码**」，并明确提示"请输入与电脑端**相同的访问密码**，两台设备才会同步同一份数据"；密码通过云端身份校验才能解锁，输错会明确报错，不会再静默分裂出第二个身份。
   - 云端没有数据（真正的首次使用）→ 才显示「设置访问密码」。
2. **旧链接自动跳转**：旧链接（muRdS…）已替换为跳转页，打开后自动进入新版页面，旧收藏也能用。
3. **顶栏新增「同步」按钮**：一键"先拉云端 → 再补传本地未上传记录"，成功/失败都有提示，手机上能立刻确认同步状态。

## 测试

- 新增冒烟 v36：15/15 通过（覆盖：云端校验模式、正确密码解锁且身份与电脑端一致、错误密码拒绝、网络异常提示、老设备本地校验不受影响等）
- 回归：v35 11/11、v34 7/7、v32（认证层）20/20 全部通过

## 你现在要做的

1. 手机上打开新链接（旧链接会自动跳转）：
   **https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy**
2. 在登录页输入**和电脑端完全相同的访问密码** → 解锁后数据自动同步。
3. 如果之前在手机上用"另一个密码"进过，请先在手机上点右上角「锁定」，然后用正确密码重新解锁即可。

> 提醒：多台设备 = 同一个密码。忘记密码无法找回云端数据，请妥善保管。
