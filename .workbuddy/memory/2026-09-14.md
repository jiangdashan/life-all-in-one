# 2026-09-14 工作日志

## v40 重启后删除/修改丢失修复（11:30-12:00）
- 现象：电脑重启后，内置习惯(看书/冥想/睡觉)复活、待买(燕麦奶)复活、计划吃什么被清空。
- 根因：①合并函数只用名字黑名单过滤云端习惯定义，忽略 hiddenHabitKeys(key)，内置习惯每次同步从云端复活；②applySettingsSync 用云端整份 settings 覆盖本地，云端空 dietPlans 清空本地；③删除走 fire-and-forget 异步且无重试，未上云则云端保留→拉回。
- 修复：mergeRemoteHabits 纳入 hiddenHabitKeys 过滤；dietPlans/weeklyPlan 改并集合并；新增 pendingDeletes 队列 + syncPendingDeletes 重启重试云端删除。
- 测试：_smoke_v40_sync_safety.js 10/10；回归全绿(共145项)。部署 import_html 带11表→静态 /14/。备份 pre-v40_20260914_114410。

## v42：智能清单改分页（每页两天、倒序、最新在前）— 已部署 /17/
- 用户需求：日程统筹「智能清单」原 v41 加的滚动条不好用，改「展开展示 + 分页」，每次展示两天，按时间倒序，最新在最前。
- 改动（复用 renderDiet 成熟分页模式）：
  - `renderPlanner`：`groupByDate(filtered)` 后 `Object.entries(...).sort((a,b)=>b[0].localeCompare(a[0]))` 倒序；`PLAN_DAYS_PER_PAGE=2` 分页；`state.settings.plannerPage` 存页码；`details` 全部 `open`（展开展示）；渲染 `#plannerPager` 分页条。
  - HTML：`#plannerList` 后插入 `<div class="planner-pager" id="plannerPager"></div>`。
  - 事件：新增 `planner-prev`/`planner-next`（改 plannerPage 后 renderPlanner），放 diet-prev/diet-next 附近。
  - CSS：删除 v41 的 `#plannerList{max-height:...;overflow-y:auto}` 滚动条，新增 `.planner-pager/.planner-pager:empty/.planner-pgmid`。
- 部署：import_html.py(位置参数+--node-block-id IAP7cP1ljx7juu0hAdPdVy+--databases 11 表) → publish_page.py → **静态 /17/**；抓线上 /17/ grep 确认 plannerPager/planner-prev/planner-next/PLAN_DAYS_PER_PAGE/planner-pgmid 全命中、滚动条已移除。
- 环境注意：本轮 bash 工具 shell 环境异常（`dirname`/`cd`/`cat`/`tail` command not found），改走 PowerShell 执行部署脚本（`$token | & $py page/xxx.py --token-stdin ...`），python `-c` 一行式做提取/校验。

### v42 回归与补测试（重要）
- **回归全绿**：v40 10/10、v39 26/26、v38 8/8、v37 17/17、v36 26/26、v35 11/11、v34 7/7、v32 20/20 → 共 125 项 0 失败，改动未破坏既有功能。
- **新增 `_tools/_smoke_v42_planner_pager.js` 13/13**：页1只 2 组且倒序(实测渲染 `9月25日|9月24日`→页2 `9月23日|9月22日`)、全展开、相邻页不重复、首/末页按钮 disabled、越界页码自动收敛非空白、单页不渲染分页条、切筛选/空数据不崩且有占位。
- 为让测试可调用，把 `renderPlanner` 加进 `window.__appTest` 导出 → 源码有变，需重发布。
- **踩坑：PowerShell 部署静默失败**。用 PowerShell 传 `--databases '[{"id":...}]'` 给 import_html.py，PowerShell 破坏双引号 → 返回 `{"error":"databases JSON 格式非法"}`，但随后 publish 仍返回 OK、list artifacts 仍显示旧版本号 /17/ → 极易误判已上线。实际表现为线上静态文件字符数纹丝不动(453928)。
- **解决**：新增 `_tools/_deploy.py`（subprocess 直传 argv，含 import→publish→取版本号→抓线上静态 grep 断言，缺标记 exit 2）。改用后 → 静态 **/18/**、线上 458083 字符、6 个标记全命中，确认生效。
- 教训已写入 MEMORY 工程铁律：**判是否真的上线要看「版本号递增 + 线上字符数 + 关键标记」，不能只看 publish 返回 OK；部署一律用 _deploy.py。**

## v45：「书影音还是没有数据」排查 + renderAll 隔离加固（13:10 起）
- 用户反馈书影音模块线上为空（其他模块有数据、走 workbuddy.link 线上链接）。
- 逐层排除（服务端/代码链路全绿）：
  - 云端 70 条 media 记录完好：`query-database` 直查返回，字段 标题/类型/状态/评分/短评 齐全、`userId=83cc858d1541cf245e95277e3e3b328f1de7796fa2c4a0799ab3bce183a2f0ba`（与用户身份一致）。
  - media schema 7 列（标题/类型/状态/评分/短评/userId/日期）与 `mergeMedia` 读取键完全一致。
  - 活页节点 `IAP7cP1ljx7juu0hAdPdVy` 的 `nodes` 已含 media 表（P93GbycwxqQ0HWmvsDTWTQ）→ 无 12607 关联丢失。
  - `mergeMedia`/`renderMedia`/`runMerge` 逻辑无误，render 中间函数(renderHome/renderStorage 等)引用的 DOM 元素均存在于模板 → 无缺失元素级联抛错。
- 结论：服务端+代码静态链路无问题，问题锁定**前端运行时**（唯一无法在本环境复现的环节）。
- 加固：`renderAll` 改为 `_safeRender` 逐个 try/catch 隔离，任一模块渲染异常不再连累书影音/心情/经期/归档整体罢工；错误打到 console 便于定位。`node --check` 通过。
- 部署：`_deploy.py --markers "_safeRender"` → **静态 /22/**（459982 字符，`_safeRender` 标记 FOUND）。
- 待用户验证：Ctrl+F5 硬刷新 https://workbuddy.link/p/IAP7cP1ljx7juu0hAdPdVy；若仍空，需 F12 控制台看 media/query/12607 报错 + app 内「数据体检」结果，并确认 心情/物品/归档 是否也空（定位是级联还是 media 专属运行时故障）。
