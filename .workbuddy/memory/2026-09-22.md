# 2026-09-22 工作日志

## v72（线上 /55/）+ v73（线上 /56/）—— 用户提出的七项需求全部完成

用户一次性提了 7 条需求。v72 落 5 条（多为「数据算错」类），v73 落 2 条（新增功能类）。

### v72：五项修复

| # | 需求 | 根因 | 改法 |
|---|---|---|---|
| 1 | 日程按优先级排序 | 完全没有排序概念，清单/象限都按 createdAt | 新增 `plannerPrioRank`/`sortPlannerByPriority`；同级内未完成在前、再同级保留 createdAt，因此拖拽排序仍有效 |
| 3 | 减脂周计划新周不清空 | `applySettingsSync` 无条件 union 远端 weeklyPlan —— 对端没跑归档时，上周计划被重新合并回来 | 远端 `weeklyPlanWeekStart` 早于本机则忽略；renderAll 兜底跑一次 rollover |
| 5 | 经期周期恒显示 2 天 | `rawLen` 优先用「有记录的天数」 | 改为有末日标记时用首末跨度 |
| 6 | 物品收纳位置归类 | `location` 字段早已存在并同步（表单/云端读写都有），缺的是归类 UI | 新增位置筛选条 + 总览位置分布 |
| 7 | 番茄钟多个不累计 | `pomoFinish` 每次覆盖表单 minutes | 新增 `_pomo.pending` 累计未提交分钟 |

**关键诊断（第 5 项）**：拉云端 period 表 22 条实证——用户每月只记录「首日」和「末日」两条，中间经期日没有打卡。旧逻辑用打卡天数算经期长度，于是每个历史周期都是 2 天，均值就永远显示 2 天。修复后实测 `[2,2] → [5,6]`。

### v73：两项新增

- **时光档案日期漫游**：原来四档只能看「相对今天」。现在有 ‹ › 翻页 + 日期选择器 + 回到今天，`_archiveInRange` 与周历/月历/年汇总全部改用 cursor。
- **倒数日模块**：内置 15 个公历节日 + 10 个农历节日 + 母亲节/父亲节/感恩节；可自定义，支持农历（含闰月）与每年重复。同步走独立 meta key `richangji:countdown`，按 id 级 LWW + 墓碑，不动 settings 快照配额。

### 踩坑记录

1. **月视图翻页会丢掉「日」**：原写法先 `setDate(1)` 再 `setMonth(m)` 防溢出，结果 7/24 +1 月变成 8/1。改为保留原日、目标月天数不足才钳到月末（1/31 +1 月 → 2/28）。自动化测试抓出来的。
2. **ESM 库内嵌**：删掉 `export {...}` 与 sourceMappingURL 后直接放进主 script（同作用域 const 可用）。嵌入前用脚本核对了顶层声明名（lunarInfo/gan/zhi/nStr1…）与主脚本零冲突。
3. **旧测试误报要会分辨**：`_smoke_v31` 把 2026-09-06 写死成「今天」，时间一过就有 3 条 FAIL；手算确认改动前后该场景下 periodLens 都是 `[1]`/`[5]`，判定为日期漂移而非回归，于是把脚本改成动态基准日。`_smoke_v68` 8a 则是 v70 把「上午2点」改成「凌晨2点」后的旧断言，一并更新。
4. **`.filter` 锚点务必取原文**：第一次 `_apply_v72_b.py` 里 HTML 锚点忘了原文是压缩单行（无换行），靠 python 直接定位再断言计数才贴合。

### 验证

- `node --check` 通过；新增 v72(39)、v73(56) 冒烟。
- 全量回归：period 系列 216 项、v53 36、v57 27、v60 44、v63 32、v64 45、v65 41、v67 22、v68 28、v69 39、v70 21、v71 28、v72 39 —— 全部 0 失败。
- 部署后线上标记断言 MISS=0。


## v74 修复习惯健康页顶部「代码」+ 倒数日视觉重做（已上线 /57/）

### 用户反馈
1. 「习惯健康最上面的代码是什么？」——页面顶部出现一串 CSS 文本。
2. 「倒数日页面做的太丑了」——倒数日完全没有样式。

### 根因（两问其实是同一个 bug）
v73-B2 插入倒数日 CSS 时，锚点 `</style>` 命中的是**习惯健康视图里的内联 `<style>`**（文件里第一个 `</style>` 在 433 行主 style，第二次插入却落到了 606 行习惯页那个），而且插在了 `</style>` **之后** → 一大段 `.cd-sub{...}.cd-hero{...}.cd-row{...}` 变成可见文本渲染在习惯健康页最顶部，还留下 `</style></style>`。
连带后果：倒数日的 CSS 一条都没生效，所以页面丑。

**教训（新增铁律）**：页面里不止一个 `<style>`（`view-habits` 那个内联 style 排在主 style 之后但更靠前被 `find` 命中）。插 CSS 一律用**带上下文的唯一锚点**（如 `</style>
  <title ...`），不要裸用 `</style>`；插完必须断言"该 section 的可见 HTML 里没有 CSS 文本"。

### 修复
1. 删除泄漏块（1191 字符），把 `.cd-*` 全部放回主 style（锚点 `</style>
  <title data-page-node-id="N9uQg0dpiKTsyB4FFPFrxl"`），并加注释 `/* v74：倒数日样式已回归主 style */`。
2. 倒数日视觉重做（朴素、无彩色/阴影）：
   - Hero 改为两栏：左主卡（`cd-hero-main`：名称 17px + 38px 大数字 + 日期/农历元信息），右侧栏（`cd-hero-side`）列出接下来 2 个（`cd-hero-sm`）；≤640px 单栏堆叠。
   - 列表行：`cd-num`（等宽数字，tabular-nums）+ `cd-body`（名称/说明，单行省略）+ `cd-tag`（公历/农历）；`days<=7` 加 `.near`（边框与数字用 `--red`）；`days===0` 显示「今天」而不是 0 天。
   - 字体数字对齐用 `font-variant-numeric:tabular-nums`，避免天数跳动。

### 验证
- `node --check` 过；`_verify_v74.py` 全 FOUND，且习惯页可见 HTML 无残留 CSS、`style` 标签数=2。
- 新增 `_smoke_v74.js` 43/43；回归 v57 27、v63 32、v65 41、v67 22、v68 28、v69 39、v70 21、v71 28、v72 39、v73 56 —— 全部 0 失败。
- v73 的 B25 旧断言（检查 `cd-hero-days`）按新结构更新为 `cd-hero-count`；注意内置节日可能比测试构造的自定义项更近（9 月 22 日时国庆 9 天 < 构造的 10 天），断言不要写死天数。
- 部署 `/56/` → `/57/`，MISS 0。

### 用户反馈（未实施，仅建议）：整体样式/展示/交互「太过普通」

已给出的建议清单（**待用户拍板，尚未改代码**）：

- **P0 结构**：①移动端 `.mobile-nav` 是左侧固定 64px 竖排图标条（16 个模块、字号 8px），吃掉一整条屏幕 → 建议改底部 5 Tab（今日/记录/计划/回顾/我的）+ 分组，或可收起侧栏；②15 个 view 一次性 `renderAll` → 改按需渲染（切到才渲染），首屏只渲染 dashboard。
- **P1 视觉**：建立三档权重（主数字 28-34px tabular-nums / 常规 13-14px / 微弱 11-12px muted）；每页顶部加「一句结论 + 一个主数字」；统一页头四件套（eyebrow / 结论 / 筛选条 / 空态）；去掉边框套边框，改用 1px 细线 + 留白分组；所有数字统一 `font-variant-numeric:tabular-nums`。
- **P2 交互**：打卡按钮即时状态（不只是 toast）；列表左滑完成/删除；骨架屏；「上次同步 xx 前」。
- **P3 锦上添花**：深色模式（--paper 现为固定浅色）；各模块 30 天 sparkline（统一 1px 细线、不用饼图不用渐变）。

**必须保留**：朴素基调（不加彩色装饰/阴影/荧光）、现有文案语气（如「完成一点，就算前进」）、离线优先、数据安全铁律。

关键现状备忘：15 个 view + 1 个 dashboard（今日总览，含 生活指数/快速开始/今日节奏/连续发生/生活脉络/轻提醒）；CSS 变量 `--paper #f7f8f9 --card #fff --ink #252a31 --muted #6b7683 --line #e4e8ec --plum #33506b --red #b0524a --radius 22px --sidebar 248px`；单文件约 650KB。


## v75 移动端导航补倒数日 + P1 三档视觉层级（已上线 /59/）

### 用户反馈
1. 手机上没有倒数日模块。
2. 按上一轮给出的建议做 P1（建立视觉主次）。

### 根因（问题 1）
移动端导航 `.mobile-nav` 是**独立于侧栏 `.sidebar` 的第二份列表**（15 项）。v73 新增倒数日时只往侧栏加了按钮，移动端那份没同步 —— 所以电脑上能进、手机上永远看不到。
修：在 payback 之后插入 countdown 按钮（图标 `#i-countdown` 早已存在）。
**教训（已写进 MEMORY）**：新增模块必须同时改侧栏与 mobile-nav 两处；冒烟新增 A2/A3 断言（16 项且含 countdown）堵住复发。

### P1 落地内容
- 新增 `renderModuleInsights()`，在 `renderAll()` 内 `try{}catch{}` 调用；10 个 `_miXxx()` 各算一个主数字 + 一句结论，经 `_miSet()` 渲染到 `.mod-insight#miXxx`。
  - money 本月支出 / 比上月多·少花了 X
  - habits 今日打卡 x/y / 最长连续 N 天（否则「今天完成一点，就算前进」）
  - fitness 当前体重 kg / 比上次轻·重 X kg
  - sleep 昨晚睡了 X / 比近 N 天平均多·少睡 X
  - study 近 7 天学习 / 今天 X 分钟 · 已完成 N 个番茄
  - planner 待办未完成 N 件 / 其中 N 件今天该做了
  - home 待买 N 件 / 本月已买 N 件
  - diet 今日摄入 kcal / 已记 N 餐
  - storage N 种 / N 件在库 · 放在 N 个位置
  - payback 大件投入 ¥X / N 件在用，越用越回本
  - 无数据时容器 `hidden`，不占位。
- CSS：`.mi-eyebrow` 11px / `.mi-main b` 30px Songti（移动端 26px）/ `.mi-note` 13px；全局 `font-variant-numeric:tabular-nums`。
- `empty(message,hint)` 增第二参数 → `.empty-hint`（向后兼容，调用点不动）。

### 验证
- node --check 过；`_verify_v75.py` 46 项断言全过（含中文串落盘断言）。
- `_smoke_v75.js` 42/42；全量回归 v57 27、v63 32、v65 41、v67 22、v68 28、v69 39、v70 21、v71 28、v72 39、v73 56、v74 43、v75 42 + period 系列（28+40+83+15+28+22）全绿。
- 部署：/57/ → /58/ → /59/。

### 新坑
- **平台发布会往元素上补 `data-page-node-id`**，部署断言用整段标签串必然 MISS（`<span>倒数</span>` 也会变成 `<span data-page-node-id="x">倒数</span>`）。改用 `>倒数</span>` 这类抗插值短串。
- 本次首次部署报 MISS 后，我没有直接改代码，而是下载线上文件核对，确认改动其实已生效（mobile-nav 16 项含 countdown），只是断言写法问题。
- bash shim 继续退化（tail/head 不可用）→ 回归改用 python subprocess 汇总输出。


## v76 修复移动端文字重叠/挤压 + 倒数日闰月 checkbox 空白（已上线 /60/）

### 用户反馈（5 张手机截图）
错乱模块：记账/习惯/减脂、回本物品明细、心情月历、倒数的闰月、支出的「本月剩余/本月支出」、减脂的身体日志。
现象：**除「倒数闰月显示为空白」外，其余都是「文字重叠/挤压」**。
（注：本轮模型不支持读图，靠用户文字补充定位模块与现象。）

### 根因（5 处，全是 CSS）
| # | 位置 | 根因 | 修复 |
|---|---|---|---|
| 1 | `.metric` 卡片（本月收入/支出/剩余、今日完成等所有 metric-row） | 移动端仍 3 列窄卡片（~110px），`strong` 18px 无 `min-width:0`/换行保护，`money()` 输出 `¥12,345.67` 带千分位 → 溢出/换行挤压 | 主样式 + 移动端都给 `strong` 加 `min-width:0;overflow-wrap:break-word;word-break:break-all;line-height:1.25`，移动端字号 18→17px |
| 2 | `.record-row`（回本明细、身体日志） | `record-main` 无 `min-width:0`，`strong`/`small` 塞长文本（身体日志所有指标、回本「类别·日期·摊销方式」）grid 挤压 | `.record-main{min-width:0}` + strong/small `overflow-wrap:break-word;word-break:break-word` |
| 3 | 心情月历 `.cal-cell` | 移动端 `min-height:34px` 太矮，竖排「日号+emoji+`+N`」重叠 | 移动端 `min-height:44px;gap:1px` |
| 4 | 倒数日「闰月」checkbox | `.field input{width:100%;height:44px}` 把 `<input type="checkbox" name="leap">` 撑成 100% 宽 44px 高，把「闰月」文字挤没 → 空白 | `.cd-inline input[type=checkbox]{width:16px;height:16px;min-width:16px;flex:none;accent-color:var(--terra)}` 覆盖 |
| 5 | （隐含）倒数日农历闰月条目本身 | 逻辑无 bug，`_cdDateNote` 正确输出「农历闰X月X日」 | 无需改，冒烟 B6/B7 验证 |

### 验证
- 纯 CSS 改动，`node --check` 过（JS 未动）；主/内联 style 大括号 1177/24 全平衡。
- `_verify_v76.py` 5/5；`_smoke_v76.js` 15/15（含 renderPayback/renderMood/renderCountdown 不抛错 + 闰月「闰」字断言）。
- 部署 `/59/` → `/60/`，线上标记 MISS 0（新增 5 个 v76 标记全 FOUND）。

### 踩坑
- 冒烟测试里 `renderMoney`/`renderFitness` 是模块内部函数**不在 `__appTest` 导出表**，直接调用报 `is not a function` → 改用已导出的 `_miMoney`/`_miFitness`（`renderAll` 已覆盖完整渲染，C1 PASS 佐证无回归）。
- 判断某函数是否可测，先 grep `__appTest` 导出对象，别凭函数名猜。


## v77 四处错乱修复：月历第 7 列 / 物品明细 / 消费圆环字号 / 身体记录（代码完成，部署受阻）

### 用户反馈（4 张手机截图）
「心情月历周六列被挤出；物品明细文字被挤压；消费结构数字要自适应；身体记录文字被挤压」

### 根因与改法
| # | 位置 | 根因 | 修复 |
|---|---|---|---|
| 1 | 心情月历第 7 列（六）溢出屏外 | `.mood-calendar{grid-template-columns:repeat(7,1fr)}` —— 轨道 `min-width:auto`，而 `.cal-cell{aspect-ratio:1;min-height:44px}` 让每格 min-content 宽 = 44px，7 列最小宽 > 容器 → 撑破卡片溢出屏幕 | 主样式改 `repeat(7,minmax(0,1fr))`（轨道可收缩）；移动端格子 `min-height` 44→38px |
| 2 | 回本「物品明细」文字挤压 | 移动端 `record-row` 4 列 `38px 1fr auto 30px`，amount 列吃掉额度，`record-main`（名称 + 数码·日期·按时间·持有N天）被压成 ~110px 宽 | 移动端改 3 列 `38px minmax(0,1fr) 30px`；`record-amount` 移到 `grid-column:2;grid-row:2`（金额下移到名称下方，main 占满整行）；`+1次` 按钮 `row3`；icon/删除跨两行 |
| 3 | 消费结构圆环中心金额溢出 | SVG 固定 `font-size="20"`，内圆直径仅 80px，`¥22,937.91` 宽约 90px → 溢出圆环 | 新增 `_amtFs` 按字符宽度计算（数字 0.556 / 千分位与小数点 0.28 / `¥` 0.62，预算 72px，min 10 max 20），`¥23,937.91` → 14px |
| 4 | 身体记录文字挤压 | `small` 里 8 个指标连成一串，`line-height` 默认偏小；`word-break:break-word`（v76 加）让中文指标被从词中间截断（「体脂肪」→「体脂/肪」） | `#fitnessList small` 加 `line-height:1.6;margin-top:3px`；`fitnessRow` 每个指标包 `<span class="nb">` + `.record-main small .nb{white-space:nowrap}` —— 只在「 · 」处换行，不再断词 |

### 关键教训
- **grid `1fr` 的轨道默认 `min-width:auto`**，配合 `aspect-ratio` + `min-height` 会产生「最小内容宽」把容器撑破。7 列网格类布局一律写 `minmax(0,1fr)`。
- **`__appTest` 导出表已补齐** `renderAll/renderMoney/renderFitness/renderMoneyPie/renderMoodCalendar`（v77 加，仅 `__TESTING__` 下生效）。此前冒烟调 `T.renderAll` 报 `is not a function` 被 `if(T.renderAll)` 静默跳过 —— 断言"不抛错"要直接调具体 render，因为 `renderAll` 内部用 `_safeRender` 吞错。
- 断言旧内容前必须先确认渲染时机：`init` 在脚本加载时已渲染一次**示例数据**，直接 push `state.records` 不重新渲染 → 断言查到的是示例数据（本轮一度误判）。

### 验证
- `_apply_v77.py` 6 处替换全中；`_verify_v77.py` 12/12；主 style 大括号 1185/1185、内联 style 24/24。
- `node --check` 主 script（414065 字符）通过。
- `_smoke_v77.js` **20/20**（A 系列 CSS 落盘 7 项 + B 系列渲染与内容 13 项）。
- `_deploy.py` 的 REQUIRED_MARKERS 已更新（v76 的 `.cal-cell{min-height:44px` 改为 38px，新增 6 个 v77 标记）。

### 阻塞
部署时 `connect_open_platform` 工具报 **not available in the current environment or configuration**（`ToolSearch` 精确查询与 `queries` 两种方式都找不到；library skill 的 `runtime_context.py` 输出 `mode=client`，而 client 模式必须带 token）。按 library skill 规定「工具不可用即停，不擅自寻找其它凭证路径」，本轮**未部署**，线上仍停在 /60/。下次需先恢复该工具/重新连接资料库再执行 `_deploy.py`。




## v76/v77 移动端样式修复（v76 上线 /60/；v77 上线 /61/）

### v76（文字重叠/挤压 + 闰月空白）
- `.metric strong` 加 `min-width:0;overflow-wrap:break-word`，移动端 18→17px（长金额在 3 列窄卡溢出）。
- `.record-main` 加 `min-width:0`，strong/small 允许换行（回本明细/身体日志挤压）。
- 心情月历 `.cal-cell` 移动端 min-height 34→44px。
- 闰月 checkbox 被 `.field input{width:100%;height:44px}` 撑爆 → `.cd-inline input[type=checkbox]{width:16px;height:16px}` 覆盖。

### v77（第二轮截图四错乱）
- 心情月历周六列挤出：根因 = `repeat(7,1fr)` 轨道 min-width:auto + 格子 `aspect-ratio:1 + min-height:44` 钉死最小宽 → 改 `repeat(7,minmax(0,1fr))`，移动端格子 44→38px。
- 回本物品明细挤压：移动端 4 列改 3 列，金额 `{grid-column:2;grid-row:2}` 下移到名称下一行。
- 消费结构圆环数字溢出：新增 `_amtFs` 按字符宽度自适应字号（数字 0.556 / 千分位 0.28 / ¥ 0.62，预算 72px），替代写死 font-size=20。
- 身体记录挤压：`#fitnessList .record-main small` line-height:1.6 + 每个指标包 `.nb{white-space:nowrap}`（v76 的 word-break 会把「体脂肪」截成「体脂/肪」，指标内禁断词）。

### 流程要点
- v77 曾因 connect_open_platform 报 not available 阻塞部署（12:40 起），13:02 重试恢复，成功发布 /61/，全部标记 FOUND MISS 0。
- 给测试导出补函数（renderAll/switchView 等）时用 `__TESTING__` 守卫；冒烟 20/20。
- 又一次踩「含反引号文本用 python -c 追加」的坑：bash 把反引号当命令替换，追加内容被截断篡改 → **写 memory 日志也必须走 .py 文件**，已用本脚本重写干净版本。


## v78（线上 /62/）—— 回本金额压到下一条 + 身体日志体重重叠

### 根因（本轮最有价值的发现）
`.record-list` 是 `display:flex;flex-direction:column;max-height:520px;overflow:auto`
（桌面端定高滚动列表设计）。`.record-row` 作为 flex 子项默认 `flex-shrink:1`：
条目一多，**每一行被压缩到 min-height:62px**。v76/v77 把金额/体重移到网格第二行后
行自然高 ~80px，压缩后第二行内容溢出（行 overflow:visible）压进下一条。
表现为「每条金额都被挤到下一个物品」「体重和身体记录重叠」。

### 修法（一行 CSS）
`.record-row` 基础规则加 `flex-shrink:0` —— 行保持自然高度，容器滚动替代压缩。
纯 CSS，一行修复全部两处。

### 验证方法突破（重要，复用）
- **安装 playwright-core + 本机 Edge（免下载 Chromium）做真实布局实测**：
  `page.addInitScript(() => window.__TESTING__ = true)` 可跳过鉴权并暴露 `__appTest`
  （v77 起含 renderAll/switchView/state），evaluate 注入回本测试数据 → switchView →
  **量 getBoundingClientRect 检测行内元素/相邻行的几何重叠**，再配元素级截图。
  本次靠它把「肉眼猜 CSS」变成「数字证明压缩 62px vs 内容 79.7px」。
- npm 直装遇 EBUSY 缓存锁 → 换 `--cache` 到隔离目录重试即好。
- 截图要截 `#paybackList`/`#fitnessList` 元素本身（页面截图只拍到表单区）。
- 脚本在 `C:/Users/依易亦奕鸭/.workbuddy/binaries/node/workspace/_layout_test_v77.js`
  与 `_shot_lists_v78.js`，下轮移动端样式问题直接改数据复用。


## v79 库存总览位置筛选不生效（上线 /63/，MISS 0）

### 根因
`renderStorageOverview` 只应用分类筛选（`storageFilter`）+ 总览搜索，**从未应用 `state.settings.storageLoc`**。点位置块 → `_locClick` 设 storageLoc → `renderStorage()` 只过滤了清单列表，总览（分类卡/条形图/提醒/明细）依旧全量 → 看起来"没生效"。

### 修复（纯 JS 3 处）
1. `renderStorageOverview` 中 `renderStorageLocGroups(records)` **之后**（归类块保持全量便于切换其它位置）加 `_locF` 过滤，cats/bar/alerts/list 全部生效。
2. `storageOvFilterNote` 提示条组合显示：分类部分（原样）+ 位置部分 `位置「X」· N 种 / M 件`，位置带独立 `data-action="storage-loc-clear"` 清除按钮。
3. action 委托注册 `storage-loc-clear`：清 storageLoc → saveState → renderStorage。

### 验证
- playwright-core + Edge 手机视口实测：注入厨房3/卫生间2/无位置1 共6条 → 点「厨房」归类块，明细 6→3、归类块仍 3 组可切换且高亮、提示条正确；点清除恢复 6 条；从清单筛选条选「卫生间」总览同步过滤为 2 条。**11/11 PASS**。
- node --check 通过（script 414,627 字符）。

### 踩坑
- **改 _deploy.py 标记列表时 `s.find(']')` 会停在含 `]` 的字符串中间**（如 `.cd-inline input[type=checkbox]{...}`），把新标记插进字符串里截断列表 → 必须用「列表真实结尾的完整锚点」（如最后一项+`\n]`）定位，改完 ast.literal_eval 校验 + 全标记对主文件 grep MISS 应为 0。


## v80 web 端「立即备份」失效（上线 /64/，MISS 0）

### 根因（真实缺陷，与两端无关但两端都受影响）
`dbUpdate` 发射后不管：业务错误 code（如 50001）/网络异常只 console.warn。而 meta 类写入有 4 处「`dbUpdate(...); if(cb) cb(true);`」——更新路径**无条件谎报成功**。立即备份当天已有云端快照时走更新路径：云端更新失败 → 用户看到「已备份到本机和云端」但云端还是旧快照 → 恢复时缺数据 → 表现为「失效」。

### 修复
- `dbUpdate(databaseId, recordId, props, cb)`：识别 result.code、catch 异常、LOCAL_ONLY → cb(false)；旧调用点不传 cb 行为不变。
- `snapshotSaveCloud` 更新失败 → 回退「dbDelete + dbAdd」重建；仍失败 → cb(false) → toast「已备份到本机，云端写入失败」。
- `renderSnapshotPanel` 文案带 `_modeNote`：未连接云端时显示「（未连接云端，备份仅存本机）」，降级状态可见。
- 其余 3 处 meta 更新路径保持 fire-and-forget（行为兼容，未动）。

### 排查过程要点
- mock 桥浏览器实测基础链路全通 → 用 mock updateRecord 返回 {code:50001} 复现「谎报成功」→ 修复后如实报错。
- 注意：行为测试的 update/add 计数会被后台 syncPendingRecords（money 记录重传）和 auto-backup 污染，断言要看**结果**（metaRows=1、toast 文案），别断言调用计数。

### 用户新增铁律（已写入 MEMORY.md）
web 端与移动端功能不允许不同步。护栏：_verify_v80.py 断言 sidebar 与 mobile-nav 的 data-nav 集合完全一致（当前各 16 项）；今后新增模块/入口必须两端同加、双视口冒烟。


## v81 立即备份"按不动"（上线 /65/，MISS 0）

### 根因（三层叠加）
1. **inject.js 的 __SMART_PAGE__ 是"永真代理"**：外壳注入真实 database 实现前，`.database` 也返回可调用的 nsProxy（调用排 15s 超时队列）。`refreshDb` 的 `ONLINE = !!db` 因此**恒为 true**。
2. v54 桥就绪轮询 `ensureBridgeSync` 因此从不生效（第一次 refreshDb 就 ONLINE=true 直接 return）。
3. 桥未就绪/握手失败时点「立即备份」→ snapshotSaveCloud 的 dbFetchAll 排进 15s 队列 + dbAdd 再 15s，且点击零即时反馈 → 用户感知"按不动"。（30s 后其实会有 toast，但几乎没人等）

### 修复（4 处）
- `refreshDb` 改用 `window.__SMART_PAGE__.__realImpls__.database` 判真就绪（inject.js 注入实现必经 set 钩子写入 real）；无 `__realImpls__` 字段（非平台/mock）回退旧判定。
- `snapshotManual` 重写：`_snapManualBusy` 去重 + 点击即变「备份中…」+disabled + 25s 看门狗 + 全程 try/catch（finish 统一恢复按钮/弹 toast/刷面板）。
- `bindSnapshotPanel` 加 document 捕获委托兜底（`__snapDelegateBound` 防重复注册；与直绑双触发由 busy 去重）。
- init 绑定改隔离数组 `[startI18n,...,bindSnapshotPanel].forEach(try/catch)`——单个 bind 抛错不再拖死后续绑定。

### 验证（真实浏览器 + 真实 inject.js 握手 + mock 外壳）
- 场景 A 桥就绪：点「立即备份」→ **199ms** toast 成功、mock 云端 backup 行 n=1、面板「共 1 份快照」无离线标注。
- 场景 B 桥未就绪：→ **168ms** toast「已备份到本机，云端写入失败」、本机快照保留、面板正确标注「（未连接云端）」。修复前此场景 15s+ 零反馈。
- node --check 过；_verify_v80 护栏 21/21（断言更新为兼容 v81 隔离数组形式）；线上 181 标记 MISS 0。

### 测试基建坑（复用价值）
- 平台 inject.js 路径是**绝对路径** `/page/page_comm/inject.js`，mock 服务器要按 `indexOf('inject.js')` 匹配。
- 外壳握手协议：iframe 广播 `SMARTPAGE_LOADER_READY`（含 loaderNonce）→ 父回 `SMARTPAGE_INJECT_SCRIPT` + `{loaderNonce, kind:'database', script}` → inject.js `__SMART_PAGE__.database = impl` 触发 set 钩子。
- 桥就绪时 auth 云端探测异步 → auth 按钮先显「解锁」后切「创建密码并进入」，测试要轮询等文案再点。
- 设置面板入口是 `#brandSettingsBtn`（`#brandSettings` 是 hidden 遮罩）；设置面板内容超高，点击前要 `scrollIntoView`。
- snapshotList 是异步渲染，toast 出现后立刻读 snapState 会读到旧文案，等 1.2s。
- `_verify_v80.py` 的启动链断言已更新：支持 `[startI18n, ..., bindSnapshotPanel]` 隔离数组形式。

## v81b（线上 /66/）—— 备份「没有任何成功/失败提示」双根因

用户反馈 v81 后备份仍无提示。真浏览器双视口排查出**两个叠加的层级 bug**：

1. **toast 被面板盖住**：`.toast` z-index 100 < `.settings-backdrop`（设置面板，按钮所在）z-index 120 → toast 弹了但被毛玻璃面板完全遮住。此前 e2e 只查 textContent 不查视觉层级，故漏检。修复：toast z-index → 10000（高于 confirm-mask 9999）。
2. **手机上按钮被 app-tip 挡住**：`#appTip`（「点桌面图标即可直接进入」引导条，z-index 120，DOM 在 backdrop 之后同层后来居上）在手机视口正好压在「立即备份」按钮上，`elementFromPoint` 命中其内部裸 `<span>`，用户点击全被吃掉 → 毫无反应。修复：app-tip z-index → 90（面板打开时被盖住，关闭后正常显示）。

验证：`_test_toast_z_v81b.js`（playwright+Edge），PC 1280×900 + 手机 390×844 双视口，判定 = 点击后 toast 出现 + `pointer-events` 临时开启后 `elementFromPoint` 命中 toast 本体 + 按钮 hitIsBtn。双 PASS 后部署 /66/，183 标记 MISS 0。

**教训：涉及 fixed 遮罩/浮层的功能，e2e 必须断言视觉层级（elementFromPoint 命中测试），不能只查 DOM 文本；toast 类反馈永远给最高层级。**

## v82（线上 /67/）—— 备份「云端响应超时」根因修复

用户反馈 toast 已可见但提示云端响应超时 → 看门狗 25s 触发说明云端回调没回来。根因是**快照数据量**：
- 备份快照存 meta 表（单格实测 15 万字符安全）；「立即备份」第一步 `dbFetchAll(DB_META)` **全量拉 meta**（含 30 天历史快照行，可能数 MB），桥接传输可拖过 25s。
- **修复四件套**：①新增 `dbFetchMetaFiltered`——平台 `db.query` 支持 `filter`（`{property:{property:"键",text:{equals/contains}}}`，结构见 library skill `database/params-reference.md`），按键过滤读取，外壳不支持时回退全量扫描；②快照云端**分片存储**（`SNAP_CHUNK_CHARS=120000`，超长拆 `backup:<date>#1..N` + 清单行 vv:2，短快照格式不变；snapshotLoad 按 #序号取齐拼装）；③看门狗 25s→60s 且**超时转后台不判死**（完成后补提示「后台备份已完成」）；④按钮显示快照体积（备份中(165KB)…）。
- snapshotList/snapshotRotateCloud 同步改过滤读取；rotate 按日期整组清理（含分片行）。
- e2e `_test_v82.js`：A 大快照(165KB→2 片)备份成功 + 删本地快照后从云端分片恢复成功；B 小快照单行兼容；0 page errors。v81b toast 层级测试回归 PASS。
- **教训：mock 必须对齐平台能力（filter）；测试读 toast 要轮询（2.4s 自动消失）；恢复链路要验证「从云端读取」而非只看写入成功。**


## v84（线上 /70/）—— 每次刷新弹补传/清理提示 + 两端条数漂移（447 vs 444）

用户问「提示的数据是真的吗？web 447 / 移动 444 为什么不同步」。先实测量化：`_probe_dupes_v84.py` 全 14 表按内容指纹扫重复行 → 2160 行 0 重复（money69+plan83+fitness5+shopping7+diet67+storage147+mood18+period22+sleep21+study2+payback6 = 正好 447 = web 备份条数 = 云端全量）。数据没坏，是同步不收敛。

四个真根因（e2e 逐个实证）：
1. `syncPendingRecords` 补传链漏 sleep 类型——sleep 记录首次上传失败后永远卡本地，且每次刷新都被计入「已补传 N 条」却永远传不出去（pushSleep 存在但没接进链）。
2. `runMerge` 写 pending=11 实际派发 13 表，cb 无 once 保护 → 连发 3 次；快照可能在最后 2 张表合并前生成（快照条数不一致来源）。
3. `syncPendingDeletes.finish()` 过滤方向反了：`filter(!failedKeys)` 保留的是成功的删除条目 → 每次刷新重删一遍再弹「已清理 N 条」，永不收敛。
4. 桥慢路径（v54 轮询 → authPostUnlockRehydrate）只拉不推：init 在 ONLINE=false 时 pullAllRemote cb(false) → 补传整体跳过；桥就绪后的重拉路径没有 syncPendingRecords/syncPendingDeletes → 手机端(standalone 桥慢)滞留记录永远没有第二次上传机会（444 的来源）。另 init 里 syncPendingDeletes 嵌在 n>0 内，补传为 0 时删除重试不跑。

v84b 修复：①补传链补 sleep ②pending=13 + once 保护 ③pendingDeletes 只留失败的等重试 ④init 补传/清理不再以拉取成功为前提 + 删除重试移出 n>0 ⑤authPostUnlockRehydrate 补 syncPendingRecords+syncPendingDeletes。

e2e `_test_v84.js`：mock 必须把 _rows 持久化到 localStorage（否则 reload 后云端清空、测试失真——首轮双场景全挂的假象）。A：卡住的 sleep 刷新后 6.2s 自动补传+remoteId 回写+二次刷新零提示零重复；B：pendingDelete 3.6s 执行+队列清空+二次静默。回归 v83 备份、v81b toast 层级全 PASS。部署 /70/ MISS 0（188 标记）。

教训：同步收敛测试必须 reload 两个会话验证（单会话测不出桥时序）；「每次刷新都弹同样提示」= 重试队列不收敛 = 先查队列的增删方向；两端条数差先算云端全量对账（447 恰好=11 表行数和）再怀疑合并逻辑。


## v85（线上 /71/）—— 移动端仍 444 + 重复日程日期应为当天

用户两点反馈：①移动端仍显示 444 条；②重复日程「加入日程」后日期应为当天而非建议的未来日期。

三个改动（v85+v85b 两轮补丁）：
1. **重复日程排今天**：createNextRepeat 原用建议的 item.nextDate（未来日期），改为加入当天日期；快照/列表同步按今天归档。
2. **当天快照自动覆盖刷新**：maybeDailySnapshot 原  当天只生成一次 → 移动端那份 444 是今天早间（修复前）生成的旧数字，之后补齐也不会更新。改为本机数据条数变化时自动覆盖刷新当天快照。
3. **runMerge 单表合并异常兜底**：此前任一表 merge 抛错会导致回调永不触发、整个同步链停摆（移动端拉不全的另一来源）→ 每表 try/catch + 失败重试 + 总看门狗。

v85b（e2e 暴露的真 bug）：push 成功回调里 remoteId 走 400ms 防抖保存，页面在此窗口内刷新则 remoteId 丢失 → 下次拉取时云端与本地同时存在 → 重复 +3（违反数据零容忍）。修复：remoteId 立即落盘 saveState()。

e2e  双 PASS：A 新日程日期=今天 09-22；B 当天快照 5→8 自动刷新 + 云端 money 8 行无重复（remoteId 立即落盘生效）。回归 v84 同步收敛、v83 备份链路全 PASS。部署 /71/ MISS 0。

**教训：remoteId 回写必须同步落盘（不能依赖防抖）；push 成功后到 saveState 之间的窗口是重复数据的温床。**


## v86 / git 初始化 —— 项目上传 GitHub 准备（含敏感信息清理）

用户要求把项目上传到 GitHub 公开仓库。完成：

1. **git init + 首次提交**：356 文件 / 89658 行，分支 master，commit d4ef2bc。工作区干净。
2. **.gitignore**：排除部署输出(含token)、backups/ 目录(26个历史备份)、_tools/_main_*.js(提取产物)、.wbapp_*.genie(含本地绝对路径)、_app_inline.js、临时检查文件。
3. **清理历史 token**：memory 日志里 2026-09-04.md、2026-09-16.md 的 op_xxx 替换为占位符。
4. **移除默认邀请码明文**：旧码 RJ-U6M9-VBCV 从全项目源码消失，换新随机码 RJ-U6M9-VBCV（AUTH_INVITE_DEFAULT 与 AUTH_INVITE_HASH 摘要 bf0a8115... 同步替换，SHA256 校验一致）。已设自定义码的用户走 localStorage 分支不受影响。注意：换默认码后，靠旧默认码+密码建的独立空间换新设备用旧码验证会失败。
5. **README.md**：新增项目说明文档。

关键决策：GitHub 公开仓库 + 移除邀请码 + 清理 token + 先不推远程(本地保留)。git 身份占位：大山 <dashan@users.noreply.github.com>。

待办：推送 GitHub 需认证(无 gh CLI/SSH key/凭证)。未来可 winget 装 gh 后 gh auth login，或用户提供仓库 URL + PAT。**推送前应先重新部署线上(让线上源码也换新邀请码，否则线上仍暴露旧码)**——此步未做。


## 上传前数据安全复查（git 提交 35def68 / 96a798a）

用户要求上传前彻底核查「绝不泄漏数据、别人打开是完全干净无数据的网页」。用 Python 直接读 .git/index + git ls-files 权威扫描全部 355 个被跟踪文件，逐项比对敏感特征：

**发现的真实泄漏（已全部清理）**：
1. `_tools/_out_period.txt`（22 条经期真实记录 + userId）、`_tools/_out_shop.txt`（7 条购物真实记录 + userId）—— **删除并从 git 移除**。
2. 真实 userId `83cc858d...`（SHA256）散落在 `.workbuddy/memory/2026-09-14.md`（排查日志里作为身份一致性说明）—— 替换占位符。
3. **旧邀请码明文 RJ-VKD5-YWLY** 在 `2026-09-22.md`、`_mem_v86.py` 残留（之前换码清理时遗漏了今天新写的日志）—— 替换新码。
4. **旧邀请码摘要 2fe922e9...** 残留 4 处（_apply_v63.py / _live_v81check.html / _online_v75.html / _tmp_check.js）—— 替换占位符。

**结论**：最终 git 跟踪 355 个文件，全量扫描「真实 userId / 旧码明文 / 旧码摘要 / token / 真实数据值(raw.字段=值)」**泄漏项 = 0**。backups/ 目录 0 跟踪（.gitignore 生效）。

**经验教训**：①数据导出/探针文件（_out_*.txt、_probe_*out）是真实数据泄漏的重灾区，已补 .gitignore 规则；②换邀请码这种「全项目替换」操作，容易漏掉「之后新写的日志/脚本」——清理后必须重新全量扫描而非只查当时的文件；③区分「源码字段名」和「真实数据值」要用「字段名后跟具体值」的正则，避免误报源码里的字段定义。
