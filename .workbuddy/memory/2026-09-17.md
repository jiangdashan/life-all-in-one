# 2026-09-17 工作日志

## v48 七项改动（已上线 /25/，云表增至 12 张）
- 热力图表头对齐：旧公式 `Math.round(28-i*26/25)` 错一位 → 改为 `ago=29-i`，每 5 列标注，最右（今天）显示「今」。
- AI 菜谱扩到 42 道（早10/午11/晚11/加餐10），保证每次推荐 ≥10 道；旧版每餐仅 2-3 道、加餐时段直接空白。
- 心情页顶部三小指标移除：根因 `.metric-row{display:grid}` 覆盖了 `hidden` 属性 → 直接删 DOM，并给 renderMood 加空值保护。
- 心情月历点击日期 → 新增 `#moodDayDetail`，展示当天表情 + 心情标签 + 文字记录，可删除，再点收起。
- 库存使用频率排行：数据存 `state.settings.storageUsage`（不改云端表结构），每次点「减少」计一次使用，频次 = 累计 ÷ 跟踪天数 × 30，Top10 + 按频次/累计切换。
- 书影音搜索：新增 `#mediaSearchInput`，按名称/类型/状态/短评实时过滤。
- 新增睡眠统计模块：新建云端表 sleep = `4rjX5sVnSNNfS2mk16nR2W`（时长统一存分钟），导航插在「减脂健身」与「待买清单」之间（桌面+手机），支持日/周/月/年统计、睡眠质量评分与建议；runMerge pending 10→11，数据体检 DIAG_TABLES 增加 sleep。

## 工程要点
- 部署：`_tools/_deploy.py` 的 DB_IDS 增至 12 表；线上静态 /24/ → /25/，496957 字符，12/12 标记全 FOUND；活页 node-info 的 nodes 已确认包含 sleep 表（排除 12607）。
- 新增回归：`_tools/_smoke_v48.js`，41/41 通过。
- 教训：`_smoke_v36_crossdevice.js` 因 v46 的限流串行化（每次查询最小间隔 250ms）导致固定短 wait 不足而误报 14 失败；把 wait 提到 ≥3.5s（数据体检段 9s）后恢复到 25 通过 / 1 失败——**今后凡改云端查询节奏，都要同步检查冒烟测试的等待时长**。

## v49 四项改动（已上线 /26/）
1. **睡眠评分缺失维度归一化**：旧逻辑未填分期时 35% 权重归零、分数上限仅 65（把「没数据」误判成「睡得差」）。改为按 hasStages / hasInBed / hasDur 判定可用维度后按权重归一化；建议区缺失时提示补齐而非误报偏低；列表无在床时间效率显示「—」。
2. 库存「使用频率」面板移到「库存总览」上方（按 article 标签计数精确交换整块）。
3. **本周计划多端同步**（重点）：根因 `mergePlanList` 用 `local.concat(remote)`「先到先得」→ 本地旧版永远胜出，另一端（移动端）的勾选状态永远同步不到 web；且无 id 时用整条内容做 key，done 一变就成新条目导致重复出现。改为按 id 取 `updatedAt` 更大者（LWW 末次修改胜出）；勾选/新增写 updatedAt；删除走 `deletedPlanIds` 墓碑（snapshot + payload + apply 前置合并，跨周清空时重置）。
   - 坑：墓碑一开始只加进了 habitsPayload，而读取端读的是 META_SETTINGS_KEY（settingsPayload），必须两处都写才生效。
4. 饮食新增**拳头法对照表**：6 组 38 条（一拳碳水 / 一掌心蛋白 / 一大拇指脂肪 / 两捧蔬菜 / 一拳水果 / 一杯乳制品）+ 每餐配比提示。
- 测试：`_tools/_smoke_v49.js` 30/30；回归 v48(41) v42(13) v40(10) v39(26) v38(8) 全绿。
- 记忆：MEMORY.md 已从 12111 压缩到 10860 字符（v36–v44 合并为一行摘要），并新增「merge 一律末次修改胜出」铁律。

## v50 饮食页布局重排（已上线 /27/）
- 需求：左侧只留「每日三餐记录区域」，其余全放右侧，顺序 份量参考 → 计划吃什么 → AI菜谱推荐 → 饮食记录，高度自适应。
- 实现：用 `_tools/_patch_diet_layout.py` 做**同标签嵌套配平**的整块搬迁（`<div>`/`<article>` 深度计数），把 diet-plan-section / ai-recipe-card / fist-guide-panel 三块从 `.module-layout` 第一列搬到 `.module-main` 内部最前，保持 DOM 完整不丢属性。
  - 坑：`extract_block` 初版用 `class="xxx"` 全等匹配，fist 面板是 `class="panel fist-guide-panel"` 匹配不到 → 改为正则 `class="[^"]*\bname\b[^"]*"`。
- 去写死高度：`.recipe-list` 去掉 `max-height:360px`；`#dietList{max-height:none;overflow:visible}` 覆盖全局 `.record-list{max-height:520px}`（只改饮食页，避免影响其他模块）。
- `.module-main>.diet-plan-section/.ai-recipe-card{margin:0}`：这两个块自带 `margin:14~16px 0`，进入 flex column(gap:18px) 后间距会翻倍。
- 验证：node --check 通过；回归全绿 v49(30) v48(41) v42(13) v40(10) v39(26) v38(8)；抓线上静态断言顺序 fist<plan<ai<dietList 且均在 module-main 之后。

## v51 「计划吃什么」已吃条目复活修复（已上线 /28/）
- 用户报：「第一个和第三个昨天已经加到已吃里了，为什么又显示出来了」。
- 排查（关键：先查线上真实数据再改代码）：用 `_tools/_diag_meta_dietplans.py` dump meta 表 → 云端最新 `appSettings`
  （updatedAt=1789583668155）里 dietPlans 7 条，其中「饺子馄饨汤底+水煮蛋」「番茄咖喱土豆鱼丸肥牛+糙米饭」done 仍为 **false**，
  **实锤 done 标记被抹掉**（不是前端隐藏逻辑的问题）。
- 根因：`mergeDietPlans` = `local.concat(remote)` 先到先得。任一端 done:false 就永远胜出，把另一端 done:true 抹掉；
  随后本端 `saveState()` → `scheduleSettingsPush()` 把 done:false 推回云端 → 全端复活。与 v49 的 weeklyPlan bug 同源（同一函数族）。
- 修复三层：
  1. `mergeDietPlans` 按 `text|date` 归并 + 字段级合并，`done` 用「或」、`doneAt`/`at` 取 max/min —— 已吃不可逆。
  2. 「吃这餐」写 `doneAt`；新增计划写 `at`（创建时间，用于同日不误判）。
  3. `renderDietPlan` 自愈 `_dietPlanEatenByRecord`：三餐记录里有同名食物即视为已吃。**必须用片段匹配**——
     用户点「吃这餐」后会在预填文本上继续加东西（计划「饺子馄饨汤底+水煮蛋」实际记成「饺子馄饨汤底+咸奶茶+可可麦芬+水煮蛋2+魔芋爽6」），
     整串包含匹配不到。规则：按 `+＋,，、;；|/空白` 切片，**全部片段出现**才算；单片段用整串包含兜底；同日要求记录晚于计划创建 `at`。
- 验证（重要方法）：`_tools/_diag_diet_plan_verify.py` 把 JS 规则在 Python 里逐字复刻，直接跑**线上 51 条三餐记录 + 云端 7 条计划**，
  结果：隐藏 6 条 / 仍展示 1 条，仍展示的正是用户没吃的「蒸红薯+蒸玉米+水煮蛋」——判定与用户描述完全一致。
- 冒烟 `_tools/_smoke_v51.js` 33/33（含真实数据回归 2n）；回归全绿 v49(30) v48(41) v42(13) v40(10) v39(26) v38(8)。
- **方法论沉淀**：这类「看起来像前端显示 bug」的问题，先用云端数据核实真实字段值（done / updatedAt），
  往往一次就能定位到是同步合并把状态抹了，而不是渲染逻辑。改完再用真实数据复刻规则跑一遍验证，比只写单元测试更可信。


## v52 · 把工作台装到手机桌面（用户问「能否以 App 形式安装」）
- 调研结论（已存 MEMORY.md 铁律区）：分享链接是外壳页 + iframe；产物页被注入守卫脚本禁止顶层打开；
  数据桥 __SMART_PAGE__.database 是 postMessage 桥、依赖父页 → 脱离外壳即无数据。
  故「全屏 PWA / 原生 App（APK/IPA）」在当前托管下不可行；「添加到主屏幕」可行（一键直达）。
- 已做（线上 **/29/** v52，18/18 标记命中）：app 内「装到手机桌面」面板（复制链接 + 5 套分设备步骤 + 7 天免密说明）、
  窄屏首屏提示条（可「不再提示」/ standalone 自动隐藏）、桌面图标 meta + 内联 PNG apple-touch-icon。
- 新工具：`_tools/_make_app_icon.py`（纯标准库手写 PNG，生成 180/192/512 图标 → base64，零第三方依赖）；
  `_tools/_smoke_v52.js` 36/36；顺手补全了 0 字节的空文件 `_tools/_extract_inline.py`。
- 交付文档：`v52-装到手机桌面说明.md`。
- 待用户决策：是否试做「全屏 App 外壳页」（需另行发布一个在线应用承载外壳，且需真机实测嵌套是否可用）。

## v53 · 鸿蒙（HarmonyOS）安装引导（用户反馈「我的移动端是鸿蒙系统」）
- 线上 **/30/** v53。改动：UA 判据 `HarmonyOS|OpenHarmony|HuaweiBrowser`，且排在 Android 分支之前（HarmonyOS 4 的 UA 同时含 Android，
  否则会被误判成 Android/Chrome 给出错误步骤）；新增 harmony（华为浏览器：底部三横线菜单 → 添加到桌面）与 harmony-wx（微信 → 在浏览器打开）
  两套步骤，并附「纯血鸿蒙 NEXT = 网页快捷方式、地址栏仍在、可开全屏浏览」说明；面板新增「我的设备」下拉（8 项，localStorage 记忆，可手动覆盖自动识别）。
- 顺带修的真 bug：下拉同步时给未选中项写空 `selected` 属性（HTML 布尔属性，存在即视为选中）→ 改为 removeAttribute。
- 新工具 `_tools/_smoke_v53.js` 36/36；回归 v52 36/36、v51 33/33、v49 30/30、v48 41/41、v42 13/13 全绿。
- 两个新教训（已入 MEMORY.md 工程铁律）：① Bash 里用 `python -c` 写含反引号的 markdown 会被 shell 命令替换吃掉 → 长文本一律 Write/Edit；
  ② 部署 marker 不可跨属性连写（平台会插 data-page-node-id 与 pnid 注释），否则误报 MISS。
- 文档：`v52-装到手机桌面说明.md` 追加「v53 · 鸿蒙专属引导」章节。


## v54 · 修复鸿蒙「装到桌面」后云端数据全空（计划吃什么/本周计划/经期PIN 不显示）
- 根因：ONLINE 与 db 引用只在脚本加载瞬间依据 window.__SMART_PAGE__.database 是否就绪判一次；鸿蒙 standalone 桌面图标模式下外壳与 iframe 桥接时序不同，工作台脚本常在桥就绪前执行 -> ONLINE 永久 false -> 所有云端读取（applySettingsSync/readMetaValue/dbFetchAll）被 if(!ONLINE) 静默跳过 -> settings 里的 dietPlans/weeklyPlan/periodPinHash 全空；经期 PIN 哈希为空则输入任何都「错误」。
- 修复：db/ONLINE 改为 refreshDb() 动态取值；新增 ensureBridgeSync() 在 DOMContentLoaded 后启动 10s 轮询，桥就绪即刷新 ONLINE 并触发 authPostUnlockRehydrate 重拉（非平台环境/无桥则 return，不轮询不挂起）。
- 质量：node --check 通过；v53 回归 36/36；源码断言 8 项全过；线上 /31/，refreshDb+ensureBridgeSync+桥就绪轮询 标记命中。
- 验证：鸿蒙桌面图标重进（或刷新），应自动同步；若滞后等 10 秒内自动重试。


## 用户决策：桌面图标名称（v54 鸿蒙验证后）
- 用户确认：鸿蒙桌面图标名称/图标不受工作台「外观/品牌」设置控制 —— 由平台外壳页决定（架构限制，v52 调研已定论）。
- 用户选择「先接受现状」：桌面图标用平台默认名；进 app 后界面内部外观（品牌名/主题色/头像/副标题）走云端 settings 同步，v54 修桥后生效。
- 未做「全屏 App 外壳页」方案（唯一能让桌面图标显示自定义名的路径，需另行发布 + 真机实测）；未来用户若再提桌面图标自定义再评估。
- 待用户验证：进 app 后界面内部外观是否已同步（判别桥在鸿蒙 standalone 是否建立）。


## v55 · 全屏 App 外壳页（用户选「做全屏外壳方案」）
- 新建独立目录 `app-shell/`：index.html（全屏 iframe 嵌套分享链接 + 加载动画 + 12s 兜底直接打开按钮 + postMessage 名称同步监听）+ manifest.webmanifest（standalone/fullscreen）+ 图标 180/192/512（由 v52 `_icon_b64.txt` 解码）。
- 标题/图标默认「日常集」/`--plum:#33506b`；改桌面图标名需改 index.html 三处 meta + JS 常量 APP_NAME 后重发。
- 经 sites 发布为独立应用，链接 https://richangji-shell.app.workbuddy.host/ （createNewApp，userAskedToPublish 因用户此前已选该方案视为授权）。线上复核：iframe 指向工作台、PWA 配置齐全、无 X-Frame-Options 可嵌套。
- 意义：这是让鸿蒙桌面图标显示自定义名/图标的唯一路径（平台外壳页不受工作台控制）。全屏 PWA 在鸿蒙上支持程度需真机实测。
- 未改 life-all-in-one.html（不污染工作台本体），降低风险。

## v56 · 学习模块（用户选「完整版 + 新建云端表 DB_STUDY」，导航放心情打卡下方）
- 线上 **/32/** v56。新增云端表 `study wtsziKZtrYywDnYMuRNcyP`（字段 日期/科目/时长(分钟)/类型/备注/userId，建表脚本 `_tools/_create_study_db.py`，仿 v48 `_create_sleep_db.py`）。
- 实现（数据层全套 LWW+墓碑+done「或」铁律，1:1 照搬 v48 睡眠模块结构，降低出错面）：TYPE_META.study、DIAG_TABLES、pullAllRemoteInner `dbFetchAll(DB_STUDY,mergeStudy)`、mergeStudy/pushStudy/updateRemoteStudy/deleteRemoteStudy、addRecord/syncPendingRecords/deleteRecord 分发、DB_LIST 追加、renderStudy（统计：今日/近7天/累计/次数 + 最近列表）、桌面+移动端导航「学习记录」插在「心情打卡」之后、view-study 区块、i-study 图标。
- 验证：node --check 通过；`_tools/_smoke_v56.js`（linkedom 加载真实脚本 + 模拟提交 studyForm）13/13 通过；部署 `_tools/_deploy.py` marker（renderStudy/i-study/studyForm/studyList/studyStat）全命中、线上 /31/→/32/。
- **重要工程教训（已入 MEMORY.md 工程铁律）**：同一文件一次消息发多个 Edit 会竞态丢改——v56 首轮 12 个 Edit 仅少数落盘（mergeStudy/pushStudy 等全丢，导致 node 检查通过但功能缺一半）。多改动一律改用**单条 python 脚本「读-改-写」原子替换**（断言每个 anchor 存在再 replace，任一缺失即 abort），落盘后再 node --check + 部署 marker 复核。

## v56 学习类型扩展 + v56b 四象限同步修复（已上线 /33/ → /34/）
- 学习类型下拉增加「语言」「技能」：类型按自由文本存取，只改表单选项，纯标记改动，线上 /33/ 复核选项全命中。
- **日程统筹四象限鸿蒙不同步修复（v56b，线上 /34/）**：用户报鸿蒙端四象限全部堆在「重要紧急」。
  - 根因：quadrant 是"字段级本地数据"——云端 DB_PLAN 表虽有「象限」列但类型是 select，而 pushPlan 写 `{text:...}` 信封对 select 列无效；mergePlan 重建记录时也从不回读云端。Web 端本地副本一直在所以正常；鸿蒙每次全新拉取 → quadrant='' → renderQuadrant 兜底全塞进 cid=1「重要紧急」。v54 之前鸿蒙整页无数据盖住了这个更老的 bug，v54 让数据能显示后才暴露。
  - 修复三步：① update_database_field 把「象限」列 select→text（field_id=Oklmw3qy，pushPlan 已写 {text:} 信封无需改）；② mergePlan 回读 `r["象限"]`（云端优先、本地兜底，LWW 不丢数据）；③ 新增 backfillPlannerQuadrant 一次性回填（localStorage 守卫 key `richangji_planner_quad_backfilled`，250ms/条串行节流防 50001，init 后延迟触发）。
  - 踩坑：原子脚本里 unicode 转义写错（\u50a2=「傢」非「象」），`r["傢限"]` 静默落盘且 node --check 不报错——**含中文字符串的替换脚本，落盘后必须 grep 断言实际中文串**。全局 7 处「傢限」→「象限」修正。
  - 验证：`_tools/_smoke_v56b.js` 9/9（merge 回读/本地兜底/回填守卫）；真实云端往返：写带「象限」临时记录→CSV 读回确认落库→删除，复核 66 条真实记录 0 残留。
  - 注意：上轮 429 报错前那次 batch_add 实际已成功（报错≠未落库），清理测试数据时要按内容复查而不是只看一次的报错。

## v57 重复日程统计 + 自动生成下次日程（已上线 /35/）
- 需求：日程统筹统计历史全部+当前日程的重复；按每次「添加→完成」的时间间隔自动形成下次日期的待完成日程。
- 实现（锚定 `_tools/_apply_repeats.py` 原子应用，7 处改动）：
  - `toggleTask` 完成时补记 `data.doneAt`（旧记录无此字段）；
  - `analyzePlannerRepeats()`：按 title 精确分组（≥2 次才算重复，sample 排除），间隔优先 doneAt-createdAt，老记录无 doneAt 时用「完成日期差」兜底；avgDays=round(mean) 且 ≥1；nextDate=上次完成日+avgDays；
  - `maybeAutoNextRepeat(task)`：完成时若「自动生成」开关开（默认开，`state.settings.plannerAutoNext`，false 为关）且推算出 nextDate，自动 addRecord 生成待完成记录（继承 象限/清单/优先级/时间/备注），toast 提示节奏与日期；
  - `createNextRepeat(item)` 守卫：同 title 已有未完成记录 → 返回 'exists' 不生成（防堆积）；
  - UI：日程统筹页四象限面板下方新增「重复日程」面板（plannerRepeatList + autoRepeatBtn 开关），每条显示 历史次数/已完成/上次完成/建议下次日期 + 「生成下次」按钮（data-action=gen-repeat，走 _plannerRepeats[idx] 索引）；
  - renderAll 挂 `_safeRender('plannerRepeats', renderPlannerRepeats)`；点击分发器加 gen-repeat / auto-repeat-switch；__appTest 导出 4 个函数。
- 同步：自动/手动生成走 addRecord('planner')→pushPlan，象限已在 v56b 接通云端；doneAt 为本地字段（跨设备用日期差兜底），无 merge 改动、无表结构改动。
- 验证：`_smoke_v57.js` 27/27（分组/兜底/守卫/自动开关/渲染）；回归 v56(13) v53(36) v56b(9) 全绿；部署 marker（renderPlannerRepeats/analyzePlannerRepeats/plannerRepeatList/autoRepeatBtn）全命中，/34/→/35/。

## v58 回本模块（已上线 /36/）
- 需求：新增回本模块（导航在学习记录下面），字段：物品名称/分类/图标(按分类自动匹配简笔画)/购买价格/购买日期/计算均价方式(默认按时间，按次数可选)；统计：物品总数/总资产/日均成本。
- 实现（`_tools/_apply_payback.py` 原子应用，20 处改动）：新云端表 DB_PAYBACK `935dPbYYu1hSUxwV7asgxy`（购买日期/物品名称/分类/购买价格/均价方式/使用次数/userId）；record type 'payback'；全套同步（mergePayback/pushPayback/updateRemotePayback/deleteRemotePayback、addRecord/syncPending/deleteRecord 分发、DIAG_TABLES）；桌面+移动端导航；view-payback（表单+统计+列表）；图标体系 i-payback + 9 个分类简笔画 symbol（数码/家电/家具/出行/运动/服饰/美妆/图书/其他，paybackIcon() 映射+兜底），表单带图标预览；renderPayback 摊销计算：按时间 price/holdDays（holdDays=今天-购买日+1，min 1），按次数 price/uses（uses<1 显示「尚未使用」），按次数物品有「+1 次」按钮（payback-use→addPaybackUse→updateRemotePayback）；统计卡 日均成本=Σ(price/holdDays)。
- **顺带修复两个 v56 遗留 bug**：① renderStudy 从未挂进 renderAll（云端拉取合并后学习列表不重渲染）；② clearAllRemoteTables 的 DB_LIST 漏 DB_SLEEP（跨设备清空不清睡眠表）。均已在本次上线修复。
- 验证：`_smoke_v58.js` 28/28；回归 v57(27) v56(13) v53(36) v56b(9) 全绿；部署 marker（renderPayback/paybackForm/paybackList/paybackStat/i-payback）全命中，/35/→/36/。
- 测试坑（已知）：linkedom 的 select.value 只读且默认 selectedIndex=-1 → 表单 select 永远读到空串，靠首项兜底或直接种记录绕过。

## v58b 导航改名（已上线 /37/）
- 用户要求「回本」「书影音」改成 4 字名，选定：回本→**回本记录**、书影音→**书影音库**。
- 8 处改名（python 单次读-改-写+逐锚点断言）：双端 nav span ×4、view-media 的 data-title、TYPE_META.payback、_tlMeta media 兜底 label、DIAG_TABLES 两项 name、EN 标题映射「书影音」→「书影音库」/Media Library。
- 页面内的句子文案（加入我的书影音/搜索书影音/toast 等）保持不动。
- 线上复核注意：平台会给 nav span 注入 data-page-node-id，精确串 `<span>回本记录</span>` 会 MISS，用 `回本记录</span>` 短标记（沿用 marker 短串铁律）。node --check 通过，/36/→/37/。

## v58c 导航名分端展示（已上线 /38/）
- 用户反馈：web 端 4 字、移动端短名。定稿：桌面端「回本记录 / 书影音库」，移动端导航「回本 / 书影音」。
- 改动：本地 payback 双端 span 文本相同（无 pnid）→ 按第 2 次出现定位改移动端；media 移动端 span 有独立 pnid（CLSLIfrfEFDU8Ye8Qlsouq）→ 精准替换。TYPE_META/体检名等共享文案保持 4 字不动。
- 线上验证方法：正则抓 `data-nav="payback|media"` 按钮的 span 文本，前 2 个=桌面、后 2 个=移动（文档顺序桌面在前）。/37/→/38/。
- 注意：用户此前报「移动端还是旧名」实为端侧缓存（线上 /37/ 已是新名），重启 standalone 即可。

## v58d 饮食页面板排序（已上线 /40/）
- 用户要求「计划吃什么」放在「份量参考」上面。两块在 view-diet 的 module-main 内原为相邻块（fist-guide-panel 在前、diet-plan-section 在后）。
- 教训：交换相邻块时第一次实现只插入未删除原块，导致 diet-plan-section 重复（同 data-page-node-id 出现 2 次）；靠 `class="..."` 计数断言（而非含 CSS/JS 引用的裸类名计数）发现并删除第二份。**移动 HTML 块必须「先取后删再插」或事后断言元素级唯一性**。
- 断言注意：`fist-guide-panel`、`diet-plan-section` 等类名在 CSS/媒体查询里也有，裸类名 count 断言会误报；用 `class="xxx"` 属性形态计数。
- 线上复核：plan 唯一且 fist/ai 顺序正确，dietPlanInput/List 在。/38/→/40/（/39/ 被平台版本号跳过）。
