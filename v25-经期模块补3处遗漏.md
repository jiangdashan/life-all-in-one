# v25 变更说明：经期模块补 3 个遗漏

**发布时间**：2026-09-06 下午
**在线链接**：https://workbuddy.link/p/muRdSFuIXga85vsZfTsTJ4
**强制刷新**：Ctrl+F5 / Cmd+Shift+R（静态资源已切到 `/25/`）

---

## 背景

v24 经期模块上线后，30+ 冒烟断言全过，但用户实际使用时发现 3 个问题。
根因是冒烟测试"直接调 addRecord 绕过 submit handler"，无法暴露真实路径的缺陷。

---

## 修复清单

### 🚨 1. 严重：表单提交按钮完全无效

**问题**：「今日身体状况」表单的「保存今日记录」按钮点击后什么都不发生——没保存、没 toast。

**根因**：v24 给记账/健身/计划/待买/饮食/物品/心情/习惯设置/计划设置/健身档案/外观 **11 个表单都加了 submit 监听**，但漏了经期表单 `periodForm`。

**修复**（line 2346-2362）：在 bindForms() 末尾绑定真实 submit 事件——
- 从 `state._periodForm` 读取经期状态/分泌物/症状/卫生巾/安睡裤
- 从 form 读取日期/心情值/心情标签/出血量/备注
- 至少一项才允许保存（防止空白记录）
- 保存成功 → `_periodResetForm()` 重置 + `renderPeriod()` 刷新

### ⚠️ 2. 心情滑块拖动时数字不变

**问题**：拖动「心情自评 0-10」滑块，右边的数字永远停在 "5"。

**修复**（line 2364-2368）：补 `periodMoodScore` 的 `input` 监听，实时同步显示文本。

### ⚠️ 3. 时光档案里经期记录显示成"生活"+购物车图标

**问题**：在「时光档案」时间线看到的经期记录标签是"生活"，图标是购物车——不是「经期」+月牙花瓣。

**根因**：`TYPE_META` 字典没加 period 类型，`_tlMeta('period')` 走默认 fallback。

**修复**（line 1435）：加 `period:{label:'经期',icon:'i-period',tone:'plum'}`。首页「今日概览」最近 5 条记录也跟着正常显示。

---

## 验证

### 冒烟测试（`_tools/_smoke_v25_period_fix.js`）

**16/16 断言全 OK**：

| 断言 | 结果 |
|---|---|
| `v25_submit_saves`：真实 submit 事件触发后 state.records 增长 1 条 | ✅ |
| `v25_record_fields`：新记录字段齐全（isStartDay/discharge/pad/moodScore 全对） | ✅ |
| `v25_form_reset_after_submit`：提交后 `_periodForm` 回到默认 | ✅ |
| `v25_blank_rejected`：空表单拒绝（不增加记录） | ✅ |
| `v25_score_updates_to_8 / _to_2`：滑块拖动实时显示对应数字 | ✅ |
| `v25_type_meta_period_in_source`：源码含 period TYPE_META | ✅ |
| `v25_archive_label_is_经期`：档案视图用 "经期" 标签 | ✅ |
| `v25_archive_uses_i_period`：档案视图用 i-period 图标 | ✅ |

### 语法 & 发布
- `node --check` ✅ 通过
- `import_html.py` + `publish_page.py` ✅ 成功
- 线上 grep 4 项标记全命中：TYPE_META period / periodForm submit / mood slider input / v25 注释
- 静态资源版本：**`/25/`**

---

## 修复后如何使用

1.强制刷新（Ctrl+F5 / Cmd+Shift+R）以加载 `/25/` 静态资源
2. 侧边栏点「经期」
3. 输入 PIN 解锁
4. 在「今日身体状况」填表 → 点「保存今日记录」→ 数据进入 state.records，自动上云（DB_PERIOD）
5. 在「时光档案」可看到经期记录以"经期"标签和月牙花瓣图标显示

---

## 教训记录（已写入 MEMORY.md 铁律 27/28）

1. **冒烟测试不要"直接调 addRecord"绕过 submit handler** —— 真实路径里的 preventDefault / 数据序列化 / 表单 reset / 错误 toast 全测不到。
2. **新增 record type 后必须立刻补 TYPE_META + titleFor + detailFor + valueFor 分支**，否则在档案/首页会以 fallback 默认值出现。