# v27 — 经期同日合并 + starts 去重

## 改了什么

### Bug 1：`addRecord('period', ...)` 多次保存累加 = 多条 records
- 用户每天多次保存（首日 / 经期中 / 末日），每次都生成独立 record
- state.records 累积多条同日期记录
- analyzePeriod 的 starts 数组出现同日重复 → UI 显示 N 行「进行中」

### Bug 2：`Object.assign({}, old, new)` 被空字符串覆盖
- 第 2 次保存 data.isStartDay='' 覆盖了第 1 次的 'Y'
- 第 2 次保存 data.discharge='' 覆盖了之前的'粘稠'
- 合并后 data 字段全空

## 修复

### 1. `addRecord('period', date, data)` 改为「同日合并」（line 1605-1633）
```js
if(type==='period'){
  var existing = state.records.find(r => r.type==='period' && r.date===date);
  if(existing){
    /* pickNonEmpty：非空字段才覆盖 */
    var _upd = {};
    for(var _k in data){
      var _v = data[_k];
      if(Array.isArray(_v)) _upd[_k] = _v;
      else if(_v !== '' && _v !== null && _v !== undefined) _upd[_k] = _v;
    }
    existing.data = Object.assign({}, oldData, _upd, {
      symptoms: Array.from(new Set(oldSym.concat(newSym)))
    });
    existing.createdAt = Date.now();
    pushPeriod(existing);  /* 复用 existing.remoteId → upsert 到云端 */
  }
  /* 否则首次记录走原路径 push 新 rec */
}
```

### 2. `analyzePeriod` 的 starts 去重（line 2117）
```js
var starts=Array.from(new Set(records.filter(r=>r.data&&r.data.isStartDay==='Y').map(r=>r.date))).sort();
```

## 验证

| 测试 | 结果 |
|---|---|
| `_tools/_smoke_v27_period_merge.js`（v27 新增 14 项 + v26 旧 26 项） | **40/40** ✅ |
| `_tools/_smoke_v25_period_fix.js` 回归 | 16/16 ✅ |
| `_tools/_smoke_v24_period.js` 回归 | 0 FAIL |

## 发布

- 静态资源：**/27/**
- 4 项标记 grep 全部命中：v27 同日合并注释 / starts=Array.from(new Set / _upd 非空字段过滤 / new Set(oldSym.concat(newSym)
- 链接：https://workbuddy.link/p/muRdSFuIXga85vsZfTsTJ4

## ⚠️ 用户需要做的

**v27 不会自动清理你现有的脏数据**——它只确保以后不再产生。

你需要手动清理：

1. **强制刷新**（Ctrl+F5）加载 /27/ 新版静态资源
2. **进经期模块**，看「周期历史」列表
3. 同日期出现多条 → 留 1 条最完整的（既有首日又有末日的），删其他的
4. 清理完后会立刻准确：
   - starts 1 个（不再重复）
   - periodLens 正确（末日标记或首末跨度补全）
   - avgPeriod / avgCycle 基于真实历史算

## 新增铁律

1. **新增 record type 前想清楚"同日多次保存"语义**：默认应当合并而不是 push。addRecord 加 type 分支时先确定：该 type 是"日同唯一多条"还是"日同可堆叠多条"？
   - 经期/打卡/心情/饮食：日同一条（增改更新）
   - 记账/日程/待买/书影音/物品：日同多条（按事项独立）
2. **合并字段策略 pickNonEmpty**：空字符串 `''` / null / undefined 不覆盖旧值；数组 union 去重；数值由用户最后写入胜出。绝不能 `Object.assign({}, old, new)` 直接覆盖，否则表单默认值会冲掉用户的真实记录。
3. **starts/periodEnds 等日期数组必须 `Array.from(new Set(...)).sort()` 去重**。历史脏数据可能已有重复，filter().map() 不会自动去重。

## 给设计者的提醒

"显示的是 2 天" 也合理：首日 9-06 + 末日 9-07 时，lastEndDay=9-07，periodDayCount=0（没勾"经期中"打卡），spanDays=2 → len=2。如果用户希望 5 天，应该补录 9-08/9-09/9-10 的经期中打卡，或第 6 天再标末日。

v27 算法逻辑没变（这是 v26 行为），v27 只解决"脏数据导致重复"和"合并字段被覆盖"两个新 bug。
